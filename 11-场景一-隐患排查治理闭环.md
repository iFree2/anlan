# 场景一：隐患排查治理闭环 —— 全链路设计种子素材

> 设计原点：**行业痛点**，而非 JD。本场景是安全生产的命脉业务，行业壁垒来自：隐患描述非结构化、判级依据是法规文本、判级错误有法律责任。
> 使用说明：所有 🔴 为示例占位，使用前必须替换为真实数据/真实经历。

---

## 1. 场景定位与业务价值

### 1.1 行业痛点（为什么是这个场景）

1. **描述非结构化**：一线排查员用手机上报，口语化、方言化、缺字段——「3号仓门口那个罐子好像有点漏」「配电箱门没关」；
2. **判级靠老师傅**：隐患分级（一般/较大/重大）依据《工贸企业重大事故隐患判定标准》等法规，新安全员拿不准，老师傅经验不沉淀；
3. **判级错误担责**：重大隐患按一般处理，出事追责——判级是「红线动作」，不是「建议」；
4. **闭环断链**：检查 → 整改 → 复查 → 销号经常断链，整改超期无人督办，复查靠人肉翻照片；
5. **数据沉睡**：历史隐患记录散落在 Excel/旧系统，没有变成知识资产和评测资产。

### 1.2 Agent 的业务价值

| 价值点 | 说明 |
|---|---|
| 判级提效 | AI 初判 + 依据条款，安全员从「翻标准」变为「确认」，判级效率提升 10 倍级 |
| 一致性兜底 | 同一类隐患不同说法，判级结果一致（双通道仲裁，见 §6 坑#1） |
| 闭环督办 | 超期自动升级，闭环率可度量 |
| 资产沉淀 | 隐患库 → 判级评测集 → 模型持续优化（数据飞轮） |

---

## 2. 业务规则需求规格（Spec，细到可开发）

### 2.1 角色与职责

| 角色 | 职责 | 系统权限 |
|---|---|---|
| 排查员 | 上报隐患（文字/语音/拍照） | 上报、查看自己上报记录 |
| 安全员 | 确认/修正 AI 判级，重大隐患现场核实 | 判级确认、转整改 |
| 整改责任人 | 认领工单、上报整改（照片+说明） | 查看/认领/上报整改 |
| 复查员 | 现场复查、确认销号或打回 | 复查、销号、打回 |
| 安全主管 | 督办超期、重大隐患升级上报 | 督办、升级、统计 |

### 2.2 主流程（状态机）

```
REPORTED(已上报)
   │ AI结构化抽取+判级
   ▼
PENDING_CONFIRM(待确认) ──AI置信度≥0.9且双通道一致──► AUTO_GRADED(自动判级)
   │ 安全员确认/修正
   ▼
GRADED(已判级) ──转整改──► ASSIGNED(待整改)
   │ 整改责任人认领/上报整改
   ▼
RECTIFYING(整改中) ──AI复查初判──► PENDING_REVIEW(待复查)
   │ 复查员现场复查
   ├── 通过 ──► CLOSED(已销号)
   └── 不通过 ──► RECTIFYING(打回重改)
   ⚠ 任一状态超期 ──► OVERDUE(超期) ──督办──► 回到原状态
   ⚠ 重大隐患 ──► ESCALATED(已升级上报)
```

**状态流转规则**：状态机强制合法迁移（如 CLOSED 不可回到 REPORTED），所有迁移记录操作人+时间（责任追溯）。

> 全闭环细化状态机（含 DRAFT 草稿 / MERGED 重复合并 / REJECTED 误报 / ESCALATED 升级分支）见 `31-隐患排查上报需求规格说明书.md` §4.1——以 31 为准（评审后 v1.0），本主文档保持总体口径。
> 判级一致性：规则通道/LLM 通道/仲裁等机制见 31 §7.1（量化判定树）与 §7.3（条款库双类型 LEGAL/POLICY）。

### 2.3 核心业务规则表（判级与流转）

| 规则编号 | 规则 | 类型 | 说明 |
|---|---|---|---|
| R-01 | 判级依据 | 法规 | 重大隐患按《重大事故隐患判定标准》行业分册（工贸/危化/冶金等） |
| R-02 | 判级输出 | 强制 | 每个判级必须输出「等级 + 依据条款号 + 条款原文」 |
| R-03 | 判级通道 | 强制 | 规则通道与 LLM 通道双通道仲裁：一致→高置信；不一致→中置信转人工 |
| R-04 | 条款校验 | 强制 | LLM 引用条款号必须存在于当前生效条款库，否则判低置信（防幻觉条款） |
| R-05 | 低置信转人工 | 强制 | 置信度 < 0.8 必须人工确认，AI 不得直接出终判 |
| R-06 | 整改时限 | 制度 | 一般 15 天 / 较大 7 天 / 重大 立即停产整改（🔴 按企业制度） |
| R-07 | 超期督办 | 制度 | 超期 1 天提醒责任人、3 天提醒安全员、7 天升级安全主管（🔴） |
| R-08 | 复查定位 | 强制 | AI 复查结论是「初判建议」，销号必须人工确认（AI 不拥有终裁权） |
| R-09 | 重大隐患 | 强制 | 判重大 → 立即停产停用 + 升级上报 + 24h 内书面报告 |
| R-10 | 数据留痕 | 合规 | 全流程操作审计 ≥ 1 年，支持安监检查导出 |

### 2.4 异常分支

| 异常 | 处理 |
|---|---|
| 上报信息过少（无法定位地点/类型） | AI 追问引导补全（多轮），3 轮仍不足转人工登记 |
| 判级条款库版本更新 | 新旧版本并行 90 天（🔴），判级记录携带条款版本号 |
| 整改上报照片与声明矛盾 | 复查初判标记「存疑」，强制人工现场复核 |
| 语音上报 ASR 低置信 | 返回原文让上报人确认后再结构化 |

### 2.5 验收标准（节选）

- AC-1：判级一致性——同一语义隐患（≥10 种表述变体）判级结果一致率 ≥ 95%（🔴）；
- AC-2：条款引用合法率 100%（幻觉条款 0）；
- AC-3：闭环率（上报→销号）≥ 90%，超期督办触发率 100%；
- AC-4：AI 误销号 0 起（复查初判 + 人工终裁设计下）；
- AC-5：全流程审计可导出。

### 2.6 Agent 与 Harness 需求补充（Spec 同步）

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| FR-A1 | 引导式上报追问 | P0 | 信息缺失触发追问，每轮 ≤3 问、总轮次 ≤5（🔴），仍不足转人工登记 |
| FR-A2 | 判级可解释性 | P0 | 判级输出含「等级 + 依据条款 + 原文 + 置信度」，支持安全员追问判级理由（解释只引用已校验条款） |
| FR-A3 | 人工修正回流 | P1 | 安全员修正判级 → 自动回流评测集（source=human_corrected） |
| FR-A4 | 督办主动对话 | P1 | 超期事件 → Agent 主动发起对话提醒（督办/解释），形成业务驱动对话 |
| FR-A5 | 判级轨迹可回放 | P1 | 判级会话全链路录制，Harness 支持变更注入回放（Skill/工具桩化，无副作用） |
| FR-A6 | Skill 化封装 | P1 | 判级/复查能力封装为 Skill（声明式定义 + 评测绑定），支持灰度发布与回滚 |
| NFR-A1 | 判级响应延迟 | P0 | 规则通道毫秒级；双通道仲裁 P95 ≤ 5s（🔴），对话不阻塞 |
| NFR-A2 | 判级一致性门禁 | P0 | 表述变体组一致率 ≥ 95% 纳入发布门禁，不达标禁止发布 |
| NFR-A3 | 工具调用审计 | P0 | 判级/复查相关 Skill 与工具调用全量审计（谁在何时调了哪个能力） |
| NFR-A4 | 工具契约测试 | P1 | query_clauses 等核心工具契约测试纳入 CI（schema/参数/错误码不回归） |

---

## 3. 系统设计

### 3.1 总体架构

```
┌────────────────────────────────────────────────────────────────┐
│ 接入层：App / H5 / 语音上报（ASR）｜ 复查拍照（Exif+定位）        │
│         （排查员/安全员/复查员 以自然语言与系统交互）            │
└───────────────────────┬────────────────────────────────────────┘
                        ▼
┌────────────────────────────────────────────────────────────────┐
│ Agent 编排层（LangGraph 编排 + 行业治理扩展层）                  │
│  ├─ 会话入口：多轮对话（上报引导 / 判级解释 / 复查问答 / 督办）   │
│  ├─ 意图理解：上报 | 判级 | 查询 | 复查 | 督办                  │
│  ├─ 规划与工具调用：ReAct 式编排，只通过「业务工具」操作闭环     │
│  ├─ 会话记忆：对话上下文（短期）；经工具读取任务状态记忆         │
│  └─ 模型服务：LLM 判级语义通道 / 结构化抽取 / 复查一致性 / bge   │
└────────┬───────────────────────────────────────────────────────┘
         ▼ 工具调用（MCP 风格：Schema 校验 + 权限校验 + 调用审计）
┌────────────────────────────────────────────────────────────────┐
│ 业务工具层（Java —— 业务闭环执行底座，Agent 不可绕过）          │
│  ├─ 上报服务：隐患登记、附件落库                                │
│  ├─ 判级服务：规则通道 + 双通道仲裁 + 条款合法性校验（终判落库） │
│  ├─ 工单引擎：状态机 + 时限规则 + 超期督办                      │
│  ├─ 复查服务：证据链组装 + 人工终裁                             │
│  └─ 审计服务：全量操作留痕 + 条款版本快照                       │
└────────┬───────────────────────────────────────────────────────┘
         ▼
┌────────────────────────────────────────────────────────────────┐
│ 数据层：条款知识库(版本化) | 隐患库 | 状态事件存储 | 判级评测集   │
└────────────────────────────────────────────────────────────────┘
```

**Agent 与业务层的对接机制（4 条，面试重点）：**

1. **边界原则——Agent 是「交互与智能」层，业务工具层是「闭环与执行」层**：Agent 负责听懂人话、多轮追问、判级语义理解、判级解释、复查证据生成；业务工具层负责状态机、权限、审计、规则硬校验、终判落库。**铁律：Agent 永不直接改状态**——一切状态变更必须经业务工具层，AI 无终裁权（对应 R-08）。
2. **对接方式——工具化（MCP 风格）**：业务工具层把能力暴露为工具：`submit_danger`、`query_clauses`、`query_danger_history`、`request_grading`、`query_review_status`、`get_similar_dangers`；工具层统一做参数 Schema 校验、租户/角色权限校验、调用审计；Agent 只面向工具编程、不感知业务表结构，业务演进不破坏 Agent。
3. **判级仲裁的分层协作**（呼应 §3.3）：规则通道在业务工具层（确定性、毫秒级、可审计）；LLM 通道在 Agent 层（语义理解），候选条款通过 `query_clauses` 工具获取——**LLM 只能引用工具返回的条款，从机制上杜绝幻觉条款**；仲裁与落库在业务工具层判级服务（带 level_source 与依据条款，全量审计）。
4. **事件回流——业务驱动对话**：业务状态变化（整改超期、复查打回、判级修正）→ 事件总线 → Agent 编排层收到后**主动发起对话**（督办提醒、解释判级、询问复查），形成「业务驱动对话」闭环；人工修正判级回流评测集（数据飞轮），持续优化 Agent 判级能力。

### 3.2 核心数据模型（DDL 节选）

```sql
-- 隐患主表
CREATE TABLE hidden_danger (
  danger_id       BIGINT PRIMARY KEY,
  tenant_id       BIGINT NOT NULL,          -- 园区租户
  org_id          BIGINT, dept_id BIGINT,   -- 组织归属
  reporter_id     BIGINT NOT NULL,
  report_time     TIMESTAMPTZ NOT NULL,
  raw_text        TEXT,                     -- 原始描述（口语化原文，留档）
  audio_url       TEXT,                     -- 语音原文（可复核）
  photo_urls      JSONB,                    -- 上报照片
  -- 结构化抽取结果（AI 填充，人工可改）
  location        JSONB,                    -- {space_node_id, 描述}
  equipment       VARCHAR(64),              -- 设备类型
  danger_type     VARCHAR(64),              -- 隐患类型（词表同源，见 31 §7.2 十二类枚举，配置化）
  source          VARCHAR(16),              -- 随手报/巡检任务/专项检查/告警转化（31 §6 预留，本期=随手报）
  degree_hint     VARCHAR(32),              -- 严重程度线索
  -- 判级结果
  level           VARCHAR(16),              -- 一般/较大/重大
  level_source    VARCHAR(16),              -- RULE / LLM / HUMAN / ARB(仲裁)
  basis_clause    JSONB,                    -- [{clause_id, 条款号, 原文, standard_ver}]
  confidence      FLOAT,
  grader_id       BIGINT,                   -- 终判人（人工）
  graded_at       TIMESTAMPTZ,
  state           VARCHAR(32) NOT NULL,     -- 状态机
  state_version   INT DEFAULT 0,            -- 乐观锁/状态机版本
  created_at      TIMESTAMPTZ, updated_at   TIMESTAMPTZ
);
CREATE INDEX idx_danger_tenant_state ON hidden_danger(tenant_id, state);

-- 状态迁移审计（append-only，责任追溯）
CREATE TABLE danger_state_event (
  event_id    BIGINT PRIMARY KEY,
  danger_id   BIGINT NOT NULL,
  from_state  VARCHAR(32), to_state VARCHAR(32),
  operator_id BIGINT, operator_role VARCHAR(32),
  action      JSONB,                       -- 迁移附带的判定/说明
  created_at  TIMESTAMPTZ
);

-- 条款知识库（版本化，支撑判级与引用校验；双类型同库，见 31 §7.3）
CREATE TABLE standard_clause (
  clause_id     BIGINT PRIMARY KEY,
  clause_type   VARCHAR(8) NOT NULL,        -- LEGAL 法定条款(判重大) / POLICY 企业规则条目(判一般/较大)
  standard_ver  VARCHAR(32) NOT NULL,      -- 版本号（法规/制度更新）
  standard_name VARCHAR(128) NOT NULL,     -- 如 工贸重大事故隐患判定标准 / 我司隐患分级细则
  industry      VARCHAR(32),               -- 工贸/危化/冶金...
  clause_no     VARCHAR(32) NOT NULL,      -- 条款号（引用合法性校验用）
  clause_text   TEXT NOT NULL,
  -- 规则通道的特征规则（结构化匹配）
  rule_features JSONB,                     -- {keywords[], synonyms[], conditions[]}
  effect_from   DATE, effect_to   DATE,    -- 生效区间
  is_active     BOOLEAN
);
```

### 3.3 判级双通道仲裁设计（本场景核心模块）

```
输入：结构化抽取结果 + 原始描述 + 生效条款库

规则通道（毫秒级，确定性）：
  条款库 rule_features → 特征匹配（术语表 + 同义词扩展 + 规则条件）
  输出：候选条款集 + 匹配强度 + 判级

LLM 通道（Agent 层，秒级，语义理解）：
  候选条款由规则通道经 query_clauses 工具下发
  Prompt: 原始描述 + 候选条款集 + 「仅可引用候选条款，输出等级+条款号+依据」
  输出：判级 + 条款号 + 依据说明
  校验：条款号必须在候选条款库中存在（R-04），幻觉条款 → 置信度归零

仲裁：
  ┌ 两通道判级一致 + LLM 条款合法 ──────────► 高置信(≥0.9)，可自动判级
  ├ 两通道判级一致 + 条款合法 + 证据弱 ─────► 中置信，转人工确认（预填 AI 结论）
  ├ 两通道不一致 ─────────────────────────► 中置信，转人工（展示双方依据）
  └ 规则通道未命中 + LLM 低置信 ──────────► 低置信，强制人工 + 入「存疑队列」回流评测集
```

### 3.4 复查辅助设计（AI 定位为「证据链提供者」）

```
输入：隐患历史事件链（上报→判级→整改中→本次复查，来自状态事件表）
    + 整改照片组 + 复查照片组 + 整改说明文本

证据链生成：
  1. 时序校验：整改声明时间 vs 照片拍摄时间 vs 复查时间（时间倒挂即标记存疑）
  2. 视觉对比（CV）：整改前后照片差异（如防护罩是否安装）→ 初判
  3. 描述一致性（LLM）：整改说明与照片内容是否相符（说了装护栏照片里没有）
  4. 结论：{初判: 已整改/未整改/存疑, 证据链: [...]}  —— 只作建议
终裁：复查员现场确认，通过→销号，不通过→打回（AI 永不直接销号）
```

### 3.5 技术选型表

| 组件 | 选型 | 理由（含行业约束） |
|---|---|---|
| 抽取/判级模型 | bge-large-zh + 自研判级 Prompt + 规则引擎 | 私有化部署（数据不出园）；规则兜底判级一致性 |
| 判级评测集 | 历史隐患库清洗 + 表述变体组 | 行业核心资产，支撑一致性回归（§6 坑#3） |
| 条款库 | PostgreSQL（JSONB 特征）+ 全文检索 | 版本化 + 条款校验 |
| 照片对比 | 自研 CV 服务（差异检测）+ LLM 描述校验 | 复查证据链 |
| 状态机 | 自研（Java，事件溯源式审计） | 责任追溯硬要求 |
| 督办 | 规则引擎 + 定时任务 + 消息推送 | 超期升级 |
| 向量 | Milvus（隐患相似检索：查重、相似案例推荐） | 历史库复用 |
| 部署 | 私有化 + 信创适配（国产库/OS 可选） | 政企合规 |

### 3.6 判级 Agent 行为设计（平台编排层在本场景的落地）

**上报 Agent（引导式多轮）**
```
第一轮：只收「地点 + 发生了什么」（自由文本/语音）→ 结构化抽取
缺失字段（设备/类型/程度线索）→ 追问（每轮 ≤3 问，术语表约束选项式提问）
→ 结构化结果回显确认（「我记录为：3号仓除尘器缺泄爆装置，对吗？」）
→ 确认后调 submit_danger 工具提交
语音路径：ASR 低置信 → 原文回显确认后再结构化（防误抽取）
```

**判级 Agent（流程模板规划 + Skill 化调用 + 可解释）**
```
意图=判级 → 流程模板（确定性编排）：
  ① 结构化抽取（工具）→ ② 调用 danger_grading Skill
       （Skill 内部管线：规则通道→LLM 语义通道→双通道仲裁，
         编排 query_clauses 工具 + 模型服务）
  → ③ 落库 + 审计（工具）
可解释设计：Skill 输出判级结论携带「证据链」（条款号 + 原文 + 置信度）；
安全员追问「为什么判重大」→ Agent 基于已校验条款生成解释（解释仅引用条款原文，
不自由发挥），解释过程本身入审计。
```

**复查 Agent（证据链提供者，不终裁）**
```
输入：隐患事件链（经工具读状态）+ 整改照片组 + 复查照片组 + 说明文本
调用 evidence_chain Skill（内部步骤：时间倒挂检测（规则）→ 视觉对比（CV 工具）
→ 描述一致性（LLM））→ 输出初判建议（已整改/未整改/存疑）+ 证据链
终裁权在复查员
```

### 3.7 Harness 应用（判级一致性评测）

```
评测集（行业核心资产）：
  表述变体组：同一隐患语义 × N 种说法（按历史描述语义聚类构造）
  条款对抗组：错误条款号/过期条款混入（验证引用校验）
  人工修正回流：source=human_corrected 增量入集
L1 Skill 评测（danger_grading / evidence_chain）：
  绑定用例（判级一致率、条款引用合法率、复查证据链完整性）→ 变更门禁
  Skill pipeline/提示词/few-shot 变更 → 自动触发绑定用例回归
L0 工具契约（query_clauses / submit_danger）：
  schema / 参数用例 / 错误码契约测试，CI 每日跑（防契约回归）
门禁（发布前）：
  判级一致率 ≥ 95% ｜ 条款引用合法率 100% ｜ 幻觉条款 0
轨迹回放：
  判级会话全链路录制 → 变更注入（Skill 版本 / 条款库版本 / 记忆参数）
  → Skill 与工具桩化回放 → 一致性 diff（无副作用）
回归触发：Skill 版本 / Prompt / 规则特征 / 条款库任何变更自动触发
```

---

## 4. 实现交底（分模块实现要点）

| 模块 | 实现要点 |
|---|---|
| 引导式上报 | 第一轮只收「地点+发生了什么」→ 结构化缺失字段触发追问（每轮 ≤3 问）；语音走 ASR + 置信度回显确认 |
| 结构化抽取 | 术语表约束（地点空间树、设备类型、隐患类型枚举）；抽取置信度 < 0.6 转人工补录 |
| 判级仲裁引擎 | 规则通道先行（毫秒级，先给安全员一个「规则初判」提升响应感）；LLM 通道异步完成仲裁；双通道结果落库（仲裁过程可审计） |
| 条款库治理 | 法规更新 → 新版本 + 新旧并行 90 天 + 判级记录带版本号（事后可追溯「当时依据的哪个版本」） |
| 工单与督办 | 时限规则配置化（按等级/园区）；超期事件进督办队列 + 消息推送 + 主管看板 |
| 复查证据链 | 时间倒挂检测 → 视觉差异 → 描述一致性 → 输出证据链报告 |
| 数据飞轮 | 人工修正判级 → 回流评测集（标注 source=human_corrected）→ 定期回归 → 触发同义表述聚类扩充用例 |

---

## 5. 实现交底补充：与内嵌支撑能力的关系

| 内嵌能力 | 在本场景的位置 | 说明 |
|---|---|---|
| 记忆（任务状态） | 隐患状态机 + 事件链 | 复查必须带完整时序上下文（§6 坑#2 的根因） |
| 记忆（长期） | 排查员画像（常查区域/历史上报习惯） | 个性化追问顺序、相似隐患提醒 |
| 评测（判级评测集） | 双通道一致性回归 | 判级模型优化唯一可信依据 |
| 知识（条款库） | 判级依据 + 引用校验 | 版本化 + 生效区间 |
| 评测（LLM-as-Judge） | 判级输出质量抽检 | 与人工判级结果对齐校准 |

---

## 6. 踩坑与解法（业务特点驱动的深坑，精而深）

### 坑 #1：同一隐患换种说法，判级结果就变了——判级一致性崩塌

**背景**：判级服务 v1 用纯 LLM（单通道）判级，上线两周。
**现象**：安全员反馈「AI 判级看运气」——「除尘器没泄爆口」「除尘系统缺泄爆装置」「粉尘车间除尘没装泄爆阀」三个描述，标准里是同一条款（粉尘涉爆重大隐患），LLM 分别判出重大/一般/重大；更糟的是有一次引用了**条款库里不存在的条款号**（幻觉条款）。判级一致性是红线，因为「判重大了」整改成本高，「判一般了」出事追责。
**排查**：把历史 500 条真实隐患描述按语义聚类，构造同义表述组做回归，发现：
1. LLM 对条款关键词敏感、对语义等价不敏感（「泄爆口」「泄爆装置」「泄爆阀」匹配不同的条款或都不匹配）；
2. LLM 会「自信地编造条款号」，格式合法但库里不存在；
3. 单通道无兜底，错误直接到终判。
**解决**：
1. **双通道仲裁**（§3.3）：规则通道按「条款→特征规则」（术语表+同义词库）先判，LLM 通道只允许从规则通道产出的候选条款中引用；
2. **条款合法性校验**：LLM 输出的条款号必须在生效条款库中，幻觉条款 → 置信度归零 + 强制转人工；
3. **表述变体评测集**：按语义聚类构造「同一隐患 × N 种说法」用例组，判级一致性纳入发布门禁（一致率 < 95% 不允许上线）；
4. 不一致案例自动入「存疑队列」，每周人工复盘，反哺同义词库与条款特征。
**反思**：判级是「责任动作」不是「建议动作」——LLM 可以当「语义理解器」，但「确定性」必须由规则兜底；行业里这种「一致性」问题，通用评测集测不出来，必须用真实历史描述按语义聚类自建评测集。

### 坑 #2：AI 差点把「整改中」误判成「已整改」销号——复查丢了时序上下文

**背景**：复查初判 v1 只把「本次整改上报内容（文字+照片）」单独喂给 LLM 判断。
**现象**：某重大隐患整改上报，整改责任人上传了现场还在施工的照片、文字写「已安排整改，正在进行」，LLM 根据「已安排」判定「已整改完成」，进入销号候选——如果复查员未拦截，一个未整改的重大隐患就被销号了。
**排查**：复盘发现两个设计缺陷：
1. LLM 只看单次输入，**看不到该隐患的历史状态链**（上报 → 判级重大 → 整改中 → 本次上报），「整改中」状态本身说明未完成；
2. 没有**时间校验**：照片拍摄时间早于整改开始时间（拿旧照片充数）、或整改声明时间与复查时间倒挂，都没有被检查。
**解决**：
1. 复查必须携带**完整事件链上下文**（状态迁移历史 + 各环节时间），LLM 基于「当前状态 + 本次上报」联合判断——内嵌任务状态记忆；
2. **时间倒挂检测**：照片 EXIF/上传时间 vs 整改声明时间 vs 状态迁移时间，倒挂即标记「存疑」强制人工；
3. **产品定位修正**：AI 复查结论从「已整改/未整改」改为「初判建议 + 证据链」，销号终裁权只在复查员（R-08）——从系统设计上堵死 AI 误销号。
**反思**：合规闭环里 AI 永远只能做「证据链提供者」，终裁权在人——这个定位必须在架构上固化（结论字段叫「建议」不叫「判定」），而不是靠 Prompt 约束。同时暴露了「单次输入无状态」是对话式 LLM 在业务闭环里的天然缺陷，必须用状态记忆补齐。

### 坑 #3：历史隐患库想做评测集，发现「金标准」本身不可信

**背景**：为判级一致性回归，计划用集团历史 8 年隐患记录做评测集。
**现象**：清洗时发现：历史记录大量字段缺失（地点/类型空白）、判级结论没有记录「依据条款」（老师傅凭经验，没写依据）、同一类隐患在不同园区判级口径不一致（A 园判一般，B 园判重大）。如果直接用历史记录当「金标准」，模型会被带偏——优化方向都是错的。
**排查**：抽样 500 条由 3 位专家复核，与系统记录一致性仅约 70%——历史数据质量不足以直接做金标准。
**解决**：
1. **重建金标准**：历史数据清洗 + 专家双人标注 + 仲裁（先做 3000 条核心场景，🔴），标注必须写依据条款；
2. **新数据从源头带依据**：判级服务强制记录依据条款（R-02），人工修正也强制填条款——新数据天然可回流评测集；
3. 评测集按「来源」分层：专家重建（gold）/ 人工修正回流（human_corrected）/ 真实轨迹（real），回归时按层加权；
4. 明确「评测集治理」是长期投入：每月新增 + 每季度专家抽检校准。
**反思**：行业里最贵的不是模型而是标注——数据飞轮的前提是「金标准可信」，而历史系统的数据几乎都需要专家重建；这个认知比任何算法都重要。

---

## 7. JD 对标收口（本场景覆盖的技能点）

| JD 技能点 | 落点 |
|---|---|
| Agent 数据闭环（会话/反馈/业务结果） | 上报→判级→整改→销号闭环 + 人工修正回流 |
| Badcase 挖掘、失败归因 | 存疑队列、判级不一致归因（坑#1 复盘机制） |
| 语义化数据分析（意图/聚类/向量检索） | 上报意图识别、表述变体聚类、相似隐患检索 |
| 短期/任务状态记忆 | 隐患状态机 + 事件链（复查上下文） |
| 长期记忆/个性化 | 排查员画像、相似隐患提醒 |
| 评测集/自动评估/LLM-as-Judge | 判级评测集、一致性门禁、判级抽检对齐 |
| 企业知识记忆 | 条款库版本化 + 引用校验 |
| 多租户/权限/审计 | 园区租户隔离 + 状态迁移审计 |
| Python+SQL/Java | 抽取/判级模型 Python、场景服务 Java |

---

## 8. 面试素材

### 高频追问
1. 判级一致性怎么保证？→ 双通道仲裁 + 条款合法性校验 + 表述变体评测集（坑#1）
2. 为什么不用纯 LLM 判级？→ 判级是责任动作，确定性必须规则兜底
3. AI 会不会误销号？→ 产品定位：AI 只出「证据链+初判建议」，终裁权在人（坑#2）
4. 评测集怎么来的？→ 历史数据专家重建 + 新数据源头带依据（坑#3）
5. 条款库更新了怎么办？→ 新旧并行 + 判级记录带版本号，可追溯

### 一句话素材
> 隐患排查场景让我想明白一件事：判级这类「责任动作」，LLM 只能当语义理解器，确定性必须由规则兜底、终裁权必须留给人——我把这个原则做进了双通道仲裁和复查证据链的架构里。
