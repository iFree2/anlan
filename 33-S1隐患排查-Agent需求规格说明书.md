# S1「隐患排查与上报」Agent 需求规格说明书

> 本文档是 S1 场景的**场景级 Agent 需求规格**（Agent 行为契约），回答「这个业务环节里 Agent 怎么干、干不好怎么办」——**不重写业务规则**（见 31），**不写 Prompt 全文、不堆评测用例数据**（那是实现期/评测期交付物）。
> 定位：三层 spec 的中间层（样板）——
> ① 业务需求 spec：`31-隐患排查上报需求规格说明书.md` + `32-...评审意见.md`（要什么、规则、验收）
> ② 场景级 Agent spec：本文档（Agent 行为契约、人机协同、质量度量）
> ③ 平台级 Agent spec：`20-Agent编排层与Harness工程设计.md`（编排/Skill/MCP/Harness 通用能力）
>
> ⚠️ 所有 🔴 为「按我司制度/数据现状取值」占位；`TBD` 见 §9。

---

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 文档编号 | S1-AgentSRS-v0.9（草稿，待评审） |
| 上游 | 31（业务 SRS v1.0）、32（评审意见）、20（平台级 Agent 能力） |
| 下游 | 系统设计（11 §3.6 Agent 行为）、Skill/工具实现（20）、Harness 评测集（20 §3） |
| 读者 | Agent 研发、算法（抽取/判级）、评测、测试 |

---

## 1. Agent 化决策矩阵（防过度 Agent 化）

| 业务环节 | Agent 化模式 | 说明（为什么这样定） |
|---|---|---|
| 上报采集（文字/语音/拍照） | **Agent 主导** | 入口价值核心：把表单门槛降为「说人话」；弱网降级直报表单 |
| 引导式追问补全 | **Agent 主导** | 结构化必需字段由对话引导；5 轮上限后转人工 |
| 结构化抽取 | **Agent 主导（人可改）** | AI 抽取 + 回显确认，人逐字段可改（改判留痕） |
| 判级（初判建议） | **Agent 主导（建议级）** | 双通道 + 证据链；产出「建议 + 依据」，非终判 |
| 判级确认/修正 | **人机协作（人决策）** | 安全员确认/改判（改判须原因）；重大升至主管/分管领导（31 §3） |
| 转整改路由 | **人机协作** | 规则路由建议 + 安全员可调整责任人 |
| 整改上报 | **Agent 辅助** | 照片强制现场拍摄 + 说明；Agent 不做决策 |
| 复查初判 | **Agent 主导（建议级）** | 证据链生成（时间校验/照片对比/描述一致性）；**不终裁** |
| 复查终裁/销号 | **保留传统（人决策）** | 复查员销号/打回；重大销号在主管（31 §3） |
| 超期督办 | **Agent 主导** | 主动对话阶梯提醒（1/3/7 天）；重大超期立即升级 |
| 状态变更通知 | **Agent 辅助** | 生成通知 + 脱敏；推送渠道按基础设施 |
| 判级解释追问 | **Agent 主导** | 仅引用已校验条款解释（约束见 §4） |

**决策原则**：责任动作（终裁/销号/改判/停产停用）→ 人；知识密集型初判（抽取/判级建议/复查证据链）→ Agent 建议 + 人终裁；重复性提醒（督办/通知）→ Agent 主导。

---

## 2. 职责边界与禁区

### 2.1 Agent 可做（授权范围）

- 引导追问、结构化抽取（回显确认）、判级初判建议、判级解释、复查初判建议、超期督办主动对话、状态通知生成。

### 2.2 Agent 永不触碰（禁区，硬约束）

| 禁区 | 规则 | 来源 |
|---|---|---|
| 终裁权 | 判级终裁、销号、改判均为人工；Agent 只出「建议 + 证据链」 | 31 R-08、32 C1 |
| 状态机 | Agent 不直接改状态，一切状态变更经业务工具层（`advance_state` 等） | 20 §2.1 ⑦ |
| 敏感操作 | 停产停用通知、重大升级上报、报送备案 → 人触发 | 31 FR-6/10 |
| 副作用工具 | BROADCAST 类工具不进入 Agent 自由调用区；WRITE 类须上下文校验 + 审计 | 20 §2.1 ④ |
| 引用越界 | 判级/解释仅引用已校验条款；引用不存在 → 判低置信/拒答 | 31 FR-6/7 |

### 2.3 与平台安全边界的关系
本文档禁区是 20 §2.1 ⑦「四条铁律」在 S1 的场景化实例，二者不冲突；实现时以平台能力（副作用分级、ACL、审计）落地。

---

## 3. 人机协同模式

### 3.1 主动对话触发规则（Agent 何时主动）

| 触发 | 条件 | 动作 |
|---|---|---|
| 超期督办 | 超期 1/3/7 天阶梯 | 主动对话提醒（对象逐级：责任人→安全员→主管） |
| 判级被修正 | 安全员改判 | 告知上报人改判结果与原因（脱敏） |
| 复查打回 | 复查员打回 | 向整改责任人说明缺什么（附证据链） |
| 重大升级 | 判级=重大 | 通知主管/分管领导（停产停用 + 报告草稿提醒） |

### 3.2 转人工策略（何时把控制权交还人）

| 情形 | 转人工条件 | 交接上下文 |
|---|---|---|
| 追问失控 | 追问 ≥5 轮仍有缺口 | DRAFT 置「待人工补录」+ 已填字段 + 缺字段清单 |
| 抽取低置信 | 置信度 < 0.6 | 预填抽取结果供人工改 |
| 判级转人工 | 重大 / 不一致 / 低置信 | 预填 AI 结论 + 证据链 + 双通道对比 |
| 判级服务不可用 | 超时/降级 | 规则初判 + 标记「AI 未完成」，转人工 |
| Agent 服务整体不可用 | — | 降级「简单表单直报」（不阻塞一线） |

### 3.3 确认窗口（防误触，S1 内有限适用）
S1 无高代价 BROADCAST 动作（那是 S3），故无强确认窗口；仅「重大隐患停产停用通知」由人工触发（天然确认）。判级改判、销号等人工动作本身即确认。

---

## 4. 交互契约（接口级，提炼自 31，不写 Prompt 全文）

| 契约 | 行为要求（验收标准） | 不约束（留给实现） |
|---|---|---|
| 追问契约 | 问什么按优先级（地点→设备→类型→程度）；选项式优先；每轮 ≤3 问、总 ≤5 轮；命中重大关键词即插一条确认问 | 具体话术、选项文案 |
| 回显确认契约 | 结构化结果必须回显「我记录为…对吗」，逐字段可改；原文永久留档 | 回显 UI 样式 |
| 判级解释契约 | 仅引用已校验条款原文；不得自由发挥；解释入审计 | 解释的句式组织 |
| 督办公约 | 主动对话频率 ≤ 阶梯阈值，不得重复骚扰；内容脱敏 | 话术模板 |

> 说明：追问/回显/解释的**验收标准**在此定义，具体 Prompt 由 20 的 Prompt 治理在实现期管理，二者通过「引用合法率 100%」等指标（§7）关联。

---

## 5. 工具与 Skill 调用契约

### 5.1 Agent 可用 Skill（S1 场景）

| Skill | 用途 | 副作用 |
|---|---|---|
| danger_grading（判级） | 规则通道 + LLM 语义 + 仲裁，输出等级+条款+置信度 | READ |
| evidence_chain（复查证据链） | 时间校验 + 照片对比 + 描述一致性 | READ |

### 5.2 Agent 可用工具（副作用分级）

| 工具 | 副作用 | 说明 |
|---|---|---|
| query_clauses | READ | 候选条款下发（判级/解释引用） |
| query_danger_history | READ | 隐患历史/事件链（复查上下文） |
| get_similar_dangers | READ | 重复隐患检测 |
| submit_danger | WRITE | 提交隐患（经权限校验 + 审计） |
| query_review_status | READ | 查询复查状态 |
| advance_state | WRITE | 状态推进（业务层权威，Agent 不直接改状态） |

### 5.3 降级契约
工具不可用 → 对应能力降级：抽取失败→人工补录；判级服务超时→规则初判+转人工；Agent 整体不可用→简单表单直报。降级路径必须可追踪（打标 + 告警）。

---

## 6. 数据与语料需求（Agent 开发特有前置，最易忽略）

### 6.1 语料/数据清单

| 语料 | 用途 | 来源 | 规模目标（🔴） | 处理要求 |
|---|---|---|---|---|
| 历史隐患描述语料（口语/方言） | 抽取/判级/追问训练与评测 | 存量台账 + 新上报回流 | 核心 3000 条起步 | 去标识化；专家双标+仲裁；标依据条款 |
| 表述变体组 | 判级一致性评测 | 按语义聚类构造（同一隐患 N 种说法） | 每组 ≥10 说法 | 覆盖 12 类词表高频类型 |
| 条款库双类型（LEGAL/POLICY） | 判级依据 | 法定标准 + 企业制度细则（31 §7.3） | 全量 | 版本化 + 生效区间 + 特征配置化 |
| 术语/同义词库 | 追问选项 + 规则通道特征 | 12 类词表（31 §7.2）+ 方言/简称映射 | 覆盖高频 | 配置化维护，与词表同源 |
| 追问对话轨迹（DRAFT） | 追问策略评测 | 线上会话记录回流 | 持续累积 | 脱敏；标注「追问是否命中缺口」 |

### 6.2 判级评测集金标准建设路径（对齐 S1 坑#3 教训）

```
历史数据清洗（去重/补字段/去标识）
→ 专家双人标注（判级 + 依据条款）→ 仲裁（不一致第三方裁定）
→ 分层入库：source=labeled（金标准）/ human_corrected（人工修正回流）/ real（真实轨迹）
→ 按 12 类 × 难度分层；季度专家抽检校准
```

- **金标准不可信则一切评测失真**：新数据从源头强制记录依据条款（31 R-02），可持续回流；
- 判级一致性评测集由「表述变体组 + 条款对抗组（幻觉条款/过期条款）」构成。

---

## 7. 质量指标与评测维度（Agent 行为质量，对接 Harness）

### 7.1 指标定义（业务指标之外的 Agent 行为质量）

| 指标 | 定义 | 阈值（🔴 门禁） | 归属评测层 |
|---|---|---|---|
| 判级一致率 | 表述变体组判级一致占比 | ≥95% | L1（Skill）+ L2 |
| 条款引用合法率 | 引用条款号在库且版本正确的占比 | 100% | L1 |
| 判级解释引用合法率 | 解释仅引已校验条款的占比 | 100% | L2 |
| 追问补全率 | 追问后结构化必需字段完整占比 | ≥90%（🔴） | L2 |
| 追问超限率 | 达 5 轮上限仍缺字段的占比 | ≤5%（🔴，越低越差须调策略） | L2 |
| 转人工率 | 转人工单占比（含各原因） | 合理区间（非越低越好，🔴） | L2 |
| 用户采纳率 | 回显确认不改的比例 | ≥85%（🔴，衡量抽取质量） | L2 |
| 督办公约遵守率 | 无骚扰投诉 + 频率合规 | 100% | 运营监控 |

### 7.2 评测维度对接（不在此堆用例，只定维度）

| Harness 层 | 覆盖内容 |
|---|---|
| L0 工具契约 | query_clauses / submit_danger / advance_state 的 schema/参数/错误码契约 |
| L1 Skill 评测 | danger_grading（一致率/引用合法率/路径 A&B）、evidence_chain（证据链完整性/时间倒挂检测） |
| L2 Agent 端到端 | 上报→闭环轨迹回放 + 追问补全 + 判级解释 + 转人工兜底 + 扰动注入（工具报错/服务超时） |

> 具体 case 数据、评测脚本是 Harness 实现期交付物（20 §3），本 spec 只定义维度与阈值。

---

## 8. 失败模式与兜底

| 失败模式 | 检测方式 | 兜底路径 |
|---|---|---|
| 判级幻觉（条款号不存在） | 引用合法性校验 | 判低置信 → 转人工；回流评测集 |
| 判级误判 | 人工改判记录 + 抽检 | 改判回流评测集；抽检机制（31 FR-6 D3） |
| 解释越界（自由发挥） | 引用一致性校验 | 拒答并回退到条款原文 |
| 追问失控（循环） | 轮次计数 | 5 轮上限转人工 |
| 判级服务超时 | 延迟监控 | 规则初判 + 转人工 + 告警 |
| 抽取低置信 | 置信度阈值 | 人工补录 |
| Agent 整体不可用 | 健康检查 | 降级简单表单直报（可追踪打标） |
| 恶意输入/灌水 | 频率限制 + 验证码 | 限流（31 §9） |

---

## 9. 未决问题（TBD）

| # | 问题 | 影响 |
|---|---|---|
| TBD-1 | 历史隐患语料现状盘点（规模/质量/是否有依据条款） | §6.1 规模与金标准建设成本 |
| TBD-2 | 转人工率「合理区间」的目标值 | §7.1 门禁 |
| TBD-3 | 追问补全率/用户采纳率基线（需上线前测一版） | §7.1 阈值 |
| TBD-4 | 术语/同义词库是否已有存量（还是从零建） | §6.1 |

---

## 10. 与相关文档关系

| 文档 | 关系 |
|---|---|
| 31 / 32 | 业务需求 + 评审（本文档的规则来源） |
| 20 | 平台级 Agent spec（通用能力，本文档是其 S1 场景化实例） |
| 11 §3.6 | 系统设计层 Agent 行为（本文档落到实现层的约束依据） |
| 22 | 模型/算力选型（判级 32B/7B 路径分级与成本关联，31 FR-6 B1） |
