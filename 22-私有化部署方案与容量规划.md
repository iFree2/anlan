# 安澜平台私有化部署方案与容量规划（模型选型 + 服务器配置 + 推导依据）

> 私有化的灵魂不是「装个模型」，而是**讲得清每一块算力和存储为什么够**——从业务量推导算力，从任务难度推导模型分档，从合规约束推导信创与数据隔离。
> 使用说明：所有数字均为 🔴 示例占位（按你司真实业务量/预算替换）；「我司」代指本公司（安防行业头部企业，具备自研视觉模型能力）。

---

## 1. 业务量假设（容量推导的输入，🔴 全部替换）

| 参数 | 假设值 | 说明 |
|---|---|---|
| 覆盖园区数 | 30 个（集团级） | 试点期可按 1-10 个 |
| 活跃用户 | 安全员/排查员/管理 ≈ 2000 人 | |
| 日均会话 | 2 万次 | 含隐患上报、判级确认、审批、指挥对话 |
| 平均会话轮次 | 8 轮 | 引导式上报/判级确认偏多轮 |
| 峰值系数 | 5×（早高峰/迎检时段） | |
| 日均隐患判级 | 3000 次 | 每次判级 = 1 次重任务 LLM 调用 |
| 日均隐患拍照 | 1500 张 | VLM 识别入口 |
| 日均语音上报 | 2000 条 | ASR |
| 每轮对话 RAG 检索 | 平均 1 次 | 条款/案例/预案 |
| 日增事件日志（溯源） | 50 万条 | 隐患/作业票/事件状态迁移 |
| 轨迹录制（Harness） | 全量（日 2 万会话） | 供 Replay 回放 |

---

## 2. 模型选型清单（各环节用什么模型 + 依据）

### 2.1 LLM 大语言模型（按任务难度分档）

| 档位 | 模型 | 承担任务 | 选型依据 |
|---|---|---|---|
| 主模型 | Qwen2.5-32B-Instruct（AWQ INT4） | 判级语义通道、预审文本审核、风险分析、指挥决策增强、**Judge 裁判（固定版本冻结）** | 判级/预审是「责任动作」，需要强推理与法规理解；32B 在中文法规场景明显优于 7B；AWQ 量化后显存减半、精度损失 <1%（经 Harness 量化前后对比验证） |
| 轻模型 | Qwen2.5-7B-Instruct | 意图分类 L2、实体/结构化抽取、摘要、督办文案生成 | Harness 难度分桶显示轻任务占流量 60-70%（🔴），7B 单卡吞吐高、成本低；承担不了判级类重任务 |
| 边缘模型 | Qwen2.5-1.5B-Instruct（INT4） | 边缘 T0+ 轻增强、断网兜底（S3 弱网园区的本地问答） | 断网可用（S3 硬约束 NFR-A2）：边缘只跑确定性规则 + 轻模型，重任务回中心 |

**依据（业务角度）**：模型分档不拍脑袋——由 Harness 评测集的**难度分桶**决定：把历史任务按「规则可解 / 语义可解 / 复杂推理」分桶，统计各桶占比（轻 60-70% / 中 25% / 重 5-10%，🔴），轻桶走 7B、重桶走 32B，成本-效果联合最优。

### 2.2 多模态视觉（我司自研 + 私有化 VLM 结合）

| 环节 | 模型 | 说明 |
|---|---|---|
| 视频 AI 事件（烟火/未戴安全帽/人员聚集/违规闯入） | **我司自研视觉模型**（安防头部企业自有能力） | 直接复用公司视频中台事件能力，经 MCP 工具接入 Agent（S3 事件识别、S6 视频联动） |
| 隐患照片识别（拍照识隐患） | **我司自研图像模型（检测/分类）为主 + Qwen2.5-VL-7B 私有化兜底** | 检测类（灭火器缺失/通道堵塞/护栏破损）自研模型精度高、速度快；开放场景（描述生成、复杂场景理解）VLM 兜底 |
| 整改前后照片对比（复查证据链） | 我司自研 CV 差异检测模型 | 复用现有能力，已接入 evidence_chain Skill |

**依据**：视觉是公司核心能力——「检测类任务用自研（准、快、私有化），开放理解类用 VLM（泛化、长尾）」；不重复造轮子，也不被开源模型限制。

### 2.3 RAG 侧模型

| 环节 | 模型 | 依据 |
|---|---|---|
| Embedding | bge-m3（1024 维） | 私有化中文主流；多粒度（句/段/篇章）适配「条款-段落-案例」混合检索；对比过 Qwen3-Embedding（更强但更重，按显存预算取舍，🔴） |
| Rerank | bge-reranker-v2-m3 | 混合检索精排标配，国产卡友好 |
| 图检索 | Neo4j + GraphRAG（社区检测） | 条款-案例-园区关系检索（详见 21 §2.3） |

### 2.4 语音

| 环节 | 模型 | 依据 |
|---|---|---|
| ASR | FunASR（Paraformer-zh，私有化） | 中文/方言友好、私有化部署成熟；对比过 Whisper-large-v3（英文更强、中文一般、更重） |
| TTS（督办语音/广播话术） | FunASR 生态 / 我司自研 TTS | 可选，按需 |

### 2.5 推理引擎与量化

```
推理引擎：vLLM（PagedAttention，主流吞吐；对比过 SGLang——复杂 prompt 场景 SGLang 更优，
         但我们长 prompt 占比不高，vLLM 社区成熟度优先）
量化：AWQ INT4（权重）+ FP8 KV cache（32B 主模型）；INT8（VLM 7B）
验证：量化前后跑 Harness L1/L2 回归，指标 diff < 阈值才允许量化上线（防「省显存丢质量」）
```

---

## 3. 服务器配置方案

### 3.1 中心集群（集团级，30 园区规模，🔴 按预算调整）

| 节点类型 | 配置 | 数量 | 承载 |
|---|---|---|---|
| GPU 推理机 | 8×A800-80G PCIe（NVLink/ROCE 互联），2×64C CPU，512G 内存 | 2 | 32B 主模型 ×2 实例（TP2）+ 7B 轻模型 ×2 实例 + VLM 7B ×1 实例 |
| GPU 备选（信创） | 8×昇腾 910B-64G（CANN）或寒武纪思元 | 2 | 与 A800 同规格替换，信创验收环境 |
| 应用/编排机 | 32C / 128G / 2×NVMe 3.84T | 6 | Agent Runtime、Skill 运行时、规则引擎、LLM Gateway |
| 向量库机 | 32C / 128G / 4×NVMe 7.68T | 3 | Milvus（HNSW 索引常驻内存） |
| 数据库机 | PG 主备 2 + Redis 集群 3 + Kafka 3 + Doris 3 + Neo4j 2 | 13 | 业务/记忆/管道/指标/图谱 |
| 数据湖 | 对象存储（MinIO 或自建）→ Iceberg | 3 | 轨迹、事件日志冷存 |

### 3.2 边缘节点（每园区 1 台，弱网/断网兜底）

```
配置：1×4090-24G（或昇腾 310B 边缘卡），16C / 64G / 2×NVMe 1.92T
承载：1.5B INT4 轻模型 + 本地规则引擎 + 预案/通知名单缓存（S3 T0 断网联动）
      本地事件缓存（离线补传中心）
```

### 3.3 三档部署方案（真实项目按规模渐进）

| 档位 | 规模 | 方案 | 适用 |
|---|---|---|---|
| 试点档 | 1 园区 | 1 台 4×4090-48G（或 2×A800）；7B 主模型 + 1.5B 边缘；docker-compose 单机 | 项目验证期 |
| 标准档 | ≤10 园区 | 2 台 8×A800；14B 主 + 7B 轻 + VLM；K8s + GPU 调度 | 区域推广 |
| 集团档 | 30 园区 | 4 台 8×A800（或昇腾 910B）；32B 主 + 7B 轻 + VLM + Judge 独立实例；K8s + 每园区边缘节点 | 集团全覆盖 |

---

## 4. 容量估算推导（为什么这些卡够用——面试核心）

### 4.1 算力（从业务量 → 峰值吞吐 → 卡数）

```
① 平均并发：日会话 2 万 / 工作 8h = 0.7 会话/s
② 峰值并发：×5 峰值系数 = 3.5 会话/s
③ 峰值轮次：3.5 × 8 轮 = 28 turn/s
④ 峰值 token 吞吐：
   输入：28 × 1500 tok（指令+知识引用+历史，判级类更长）= 42,000 tok/s
   输出：28 × 300 tok = 8,400 tok/s
⑤ 单卡吞吐参考（vLLM，batch 8-16）：
   32B AWQ INT4 / A800 ≈ 输出 1,500-2,500 tok/s
   7B INT4 / A800 ≈ 输出 6,000+ tok/s
⑥ 卡数推导：
   32B：8,400 tok/s ÷ 2,000 ≈ 需 4.2 卡输出能力 → 用 8 卡（2 组 TP2 × 2 实例）
        ≈ 2 倍峰值冗余（迎检/故障切换）
   7B：轻任务占 60-70% 流量 → 2 卡（2 实例）宽裕
结论：8 卡 A800 × 2 台 = 16 卡，承载 30 园区峰值并留 2 倍冗余（🔴 按真实量重算）
```

### 4.2 显存

```
32B AWQ INT4：权重 ≈ 18GB + KV cache（16K ctx × batch 16 ≈ 8GB）+ 激活 ≈ 30GB/卡
  → 80G 卡单实例宽裕，TP2 更稳
7B INT4：权重 ≈ 5GB + KV ≈ 7GB → 24G 卡即可
VLM 7B INT8：≈ 9GB → 24G 卡即可
bge-m3（0.6B）：≈ 1.3GB → CPU 也能跑（对延迟不敏感场景）
1.5B INT4（边缘）：≈ 1.5GB → 24G 卡绰绰有余
```

### 4.3 存储

```
向量：年增隐患/案例 500 万条 × 1KB ≈ 5GB/年 → Milvus 3 台 NVMe 无压力
事件日志（溯源/审计，合规保留 ≥1 年）：日 50 万条 × 0.5KB ≈ 25GB/天
  → 热存 90 天（≈2.2TB）+ 冷存 Iceberg 1 年（≈9TB，可压缩至 3TB）
轨迹（Harness Replay 数据源）：日 2 万会话 × 20KB ≈ 400MB/天 → 年 ≈ 150GB（压缩后）
指标/时序：Doris 日增量小，3 台宽裕
```

### 4.4 网络

```
中心-边缘：专线（🔴 按园区实际带宽）；边缘断网自持 T0（规则+预案本地）
中心内部：25G 内网（GPU 互联 NVLink/ROCE）；Kafka/Iceberg 高吞吐链路
```

---

## 5. 选型依据总结（决策逻辑，面试话术）

1. **算力不拍脑袋**：容量 = 业务量 → 峰值并发 → 峰值 token 吞吐 → 单卡吞吐 → 卡数，推导链完整（§4.1）；
2. **模型分档由任务难度驱动**：Harness 难度分桶统计（轻 60-70% 走 7B），不是「一个模型打天下」；
3. **多模态分「检测/理解」**：检测类（隐患对象/视频事件）用我司自研（准、快、私有化），开放理解类用 VLM 兜底（泛化、长尾）；
4. **边缘由断网约束驱动**：S3 硬约束（T0 断网可用）决定边缘节点配置，不是所有算力堆中心；
5. **信创由合规驱动**：A800 主方案 + 昇腾 910B 备选（信创验收），国产卡适配在模型层做（CANN 算子适配）；
6. **量化由成本驱动但以评测兜底**：INT4 省显存的前提是 Harness 量化前后回归 diff < 阈值，绝不以质量换成本。

---

## 6. 面试应答（「你这配置够吗/为什么这么选」）

> 我的容量规划方法论是「先业务量、后算力」：日会话 2 万 → 峰值 3.5 会话/s → 峰值输出 8400 tok/s → 32B 单卡 2000 tok/s → 8 卡两倍冗余。模型分档来自评测集的难度分桶，轻任务六成走 7B 省成本；多模态检测用我司自研、理解用 VLM；边缘节点是为了断网也能 T0 联动。所有量化都过了 Harness 回归门禁——省显存不能丢质量。这套推导我随时能重算，换成 5 万会话或 100 园区都行。

---

## 7. 红线（真实性护栏）

1. **所有数字（会话量、峰值系数、单卡吞吐、园区数）必须按你司真实业务量重算**——面试官追问「你一天多少会话」答不上来就崩；
2. 单卡吞吐参考值（vLLM 实测区间）按你真实压测数据替换，别背通用数字；
3. 「我司自研视觉模型」按你司真实能力表述（视频 AI 检测是安防头部企业的真实资产，合理）；
4. 昇腾/寒武纪适配只写真实评估过的深度（算子适配、性能对比），没做过的写「选型评估中」。

---

## 8. 一句话素材

> 私有化部署我交得出三张底牌：模型分档（32B 判级 + 7B 轻任务 + 1.5B 边缘，难度分桶驱动）、容量推导（业务量→峰值吞吐→卡数，随时可重算）、边缘兜底（断网 T0 联动）。每一块算力都有业务依据，每一张卡都有评测背书——这才是「能交付」的私有化，而不是「装个模型」。
