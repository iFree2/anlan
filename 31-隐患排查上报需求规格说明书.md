# S1「隐患排查与上报」需求规格说明书（全闭环：上报 → 销号）

> 本文档是「安澜」平台场景一（隐患排查治理闭环）的**细化需求规格**，用于指导后续设计、编码与测试。与 `11-场景一-隐患排查治理闭环.md`（总体设计）配套：主文档管架构与系统设计，本文档管**可开发的需求条目**。
> 需求来源口径（已与业务方确认）：移动端 App/H5（文字+拍照+语音）｜全闭环｜三级判级（一般/较大/重大）+ 重大强制人工｜隐患分类词表初拟 12 类（待我司校准）｜全员可报 + 安全员主导。
> ⚠️ 本文档所有 🔴 标注为「按我司实际制度/基础设施取值」项；`TBD` 为未决问题（见 §11），编码前须裁决。
> **修订记录**：v0.9 → v1.0（需求评审后修订）：采纳评审意见 A1-A4 / B1-B3 / C1-C4 / D1-D3，详见 `32-隐患排查上报需求评审意见.md` §2。

---

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 文档编号 | S1-SRS-v0.9（草稿，待评审） |
| 适用系统 | 安澜智能体平台 · 隐患排查模块 |
| 上游依赖 | 统一身份/组织、园区空间树、隐患判级条款库、消息推送 |
| 下游消费 | 整改/复查模块（同属本文档范围）、督办统计 |
| 读者 | 产品、前后端开发、测试、算法（判级/抽取） |

---

## 1. 范围与目标

**本期范围**：一线人员通过移动端上报隐患 → AI 结构化抽取 → 双通道判级（重大强制人工）→ 转整改 → 整改上报 → 复查销号，全流程闭环；含全员参与（匿名兜底）与督办升级。

**业务目标（度量口径，🔴 取我司基线）**
- 上报参与度：月活上报人数 / 员工总数 ≥ 目标值（打破「表单门槛导致不上报」）；
- 判级一致性：同一语义不同表述判级一致率 ≥ 95%；
- 闭环率：上报→销号闭环率 ≥ 90%，超期督办触发率 100%；
- 重大隐患：判级→停产停用+升级上报 ≤ 24h 书面报告。

---

## 2. 术语

| 术语 | 定义 |
|---|---|
| 隐患 | 物的危险状态/人的不安全行为/管理缺陷 |
| 上报人 | 一线员工/外包/访客（登录或匿名） |
| 隐患描述 | 上报人对隐患的自然语言/语音描述（原始语料，必须留档） |
| 结构化抽取 | 将原始描述转为 {地点/设备/类型/程度线索} 结构化字段 |
| 判级 | 按条款/制度判定等级：一般/较大/重大 |
| 双通道仲裁 | 规则通道（条款特征匹配）与 LLM 通道（语义理解）交叉验证 |
| 证据链 | 判级依据（条款号+原文）、复查依据（照片对比+时间校验） |
| 匿名兜底 | 访客免登录上报（防「发现隐患但没账号就不报」） |

---

## 3. 角色与权限

| 角色 | 来源 | 权限（关键） |
|---|---|---|
| 普通上报人（员工/外包） | 登录（统一身份） | 上报、查看自己的记录与进度、被追问；**上报人身份对整改责任部门/责任人不可见**（C2 举报保护，见 §8 NFR-9） |
| 匿名访客 | 免登录 + 短信验证码 | 仅上报与查回执（凭随机回执号查询） |
| 安全员 | 登录 + 安全岗 | 一般/较大隐患判级确认/修正、驳回误报、转整改、督办 |
| 整改责任人 | 登录 | 认领工单、整改上报（现场拍摄照片+说明）、延期申请 |
| 复查员 | 登录 + 复查岗 | 现场复查（一般/较大销号或打回） |
| 安全主管/分管领导 | 登录 + 管理岗 | **重大隐患判级复核与销号终裁**（C1，对齐新安法 41 条组织层级）、督办升级、报送备案 |
| 系统（Agent） | — | 引导追问、抽取、判级建议、督办提醒、主动对话 |

**分角色流程差异（全员参与的核心）**：
- 普通上报人/匿名：上报 → 判级后**必须安全员确认**才可转整改（防误报/恶意上报流入工单）；
- 安全员本人上报：判级后**可直接转整改**（本人即专业确认），但自报单打「本人上报」标记，主管按比例抽查（B2）；重大隐患仍走强制人工复核；
- **重大隐患**（不论上报人）：判级复核与销号终裁在**安全主管/分管领导**，安全员仅初核（C1）。

---

## 4. 流程总览

### 4.1 全闭环状态机

```
DRAFT(会话中/追问中，草稿)
  │ 信息补全提交
  ▼
REPORTED(已上报)
  │ AI 结构化抽取（置信度回显确认）
  ▼
STRUCTURED(已结构化) ──重复隐患检测命中──► MERGED(并入既有工单，提示上报人)
  │ 双通道判级
  ▼
GRADING(判级中)
  ├── AI 高置信 + 双通道一致 + 非重大 ──► GRADED(自动判级)
  └── 重大/不一致/低置信 ──► PENDING_CONFIRM(待人工确认，预填 AI 结论+证据链)
        │ 安全员确认/修正（普通上报必需）
        ▼
GRADED(已判级) ──转整改（按类型/区域路由）──► ASSIGNED(待认领)
  │ 整改责任人认领/上报整改
  ▼
RECTIFYING(整改中) ──AI 复查初判──► PENDING_REVIEW(待复查)
  │ 复查员现场复查
  ├── 通过 ──► CLOSED(已销号·归档)
  ├── 不通过 ──► RECTIFYING(打回重改，附证据链)
  └── 误报/重复 ──► REJECTED(驳回，须填原因，可入案例库)
  ⚠ 任一环节超期 ──► OVERDUE(超期) ──督办阶梯──► 回原状态
  ⚠ 重大隐患 ──► ESCALATED(升级上报：停产停用 + 24h 书面报告)
```

### 4.2 三条主路径

1. **员工/外包上报**：上报 → 追问补全 → 结构化回显 → 判级 → 安全员确认 → 转整改（闭环全链）；
2. **安全员上报（快捷）**：上报 → 判级 →（非重大）直接转整改，跳过二次确认；
3. **匿名访客上报**：验证码 → 上报 → 追问 → 结构化 → 转「待安全员认领确认」→ 凭回执号查进度（不推送）。

---

## 5. 功能需求详述

### FR-1 上报会话启动

- 入口：App/H5 首页「随手报隐患」；支持 一键唤起相机/录音/键盘输入；
- 会话开始即建 DRAFT；上报人可中途退出，草稿保留 24h（🔴），再次进入可续填（断点续办）；
- 启动时定位：GPS（园区外不适用则手动选园区）→ 会话绑定租户/空间树上下文；
- **来源标识（C4 预留）**：隐患主表预留 `source` 枚举——随手报（本期）/ 巡检任务 / 专项检查 / 告警转化（二期接入，本期仅随手报写「随手报」）；预留枚举位避免后期改主表。

### FR-2 多输入采集（三种模式可混用）

| 模式 | 规则 |
|---|---|
| 文字 | 自由文本；首轮只要求「发生了什么事」，不要求填字段 |
| 语音 | ASR 转写（私有化 FunASR 类，🔴 按 22 文档选型）；转写置信度 < 0.7 时回显原文请上报人确认后再进结构化；录音 ≤ 60s（🔴） |
| 拍照 | 最多 9 张（🔴）；自动携带 Exif 时间 + GPS；无 Exif 时以拍摄上传时间为准（复查时间校验用）；本期不做 VLM 自动识别（预留接口，见 TBD-6） |

### FR-3 引导式多轮追问补全

- 目标：把结构化必需字段问全，**让上报人只说人话**；
- 追问对象（按信息缺口优先级）：地点 → 设备/部位 → 隐患类型 → 严重程度线索；
- 追问形式：**选项式优先**（隐患类型给词表枚举选项，地点给空间树层级选择），自由补充为辅；
- 每轮 ≤ 3 问；**总轮次 ≤ 5 轮**（🔴）；达到上限仍有缺口 → 转人工登记（DRAFT 置「待人工补录」），不阻塞上报；
- 追问中上报人可直接提交「不完整单」（系统标记缺字段，判级转人工）；
- 关键词触发：描述中命中重大条款关键词（如「粉尘涉爆」「泄爆」）→ 立即插入一条确认问题「是否涉及…？」，命中即走重大复核。

### FR-4 结构化抽取与回显确认

- 抽取字段：location（空间节点+描述）、equipment（设备/部位）、danger_type（词表枚举）、degree_hint（程度线索：严重/一般/不确定）；
- 抽取完成必须**回显确认**：「我记录为：3 号仓除尘器，类型=粉尘涉爆设施，对吗？」——上报人可逐字段修改；
- 抽取置信度 < 0.6 → 提示人工补录/改选；上报人修改记录留痕（进审计与评测回流）；
- 原始描述（文本/语音原文/照片）永久留档，不得丢弃（判级复核与取证用）。

### FR-5 重复隐患检测

- 结构化完成后，基于 {租户, 地点节点, 类型} + 语义相似（Milvus 向量，复用历史隐患描述）检索 Top3；
- 高相似（≥ 阈值，🔴）：提示「该位置同类隐患已有在办工单 #123」，**引导合并或新建并备注**；
- 误报治理：安全员可 REJECTED 驳回（须填原因）；被驳回 3 次以上同一位置同一类型 → 提示安全员现场核查后处置。

### FR-6 双通道判级（三级 + 重大强制人工 + 判级路径分级）

**判级依据来源（A2）**：条款库含两类条目、同库同版本管理（见 §7.3）——
- 法定条款（判重大依据，如《工贸企业重大事故隐患判定标准》行业分册）；
- 企业规则条目（判一般/较大依据，由企业制度细则生成规则特征，经配置化维护）。
规则通道的候选条款可来自两类；LLM 通道仅可引用规则通道下发的候选条款（防幻觉）。

**判级路径分级（B1，成本平衡）**：
| 路径 | 触发条件 | 执行 |
|---|---|---|
| A 规则裁决 | 命中重大条款特征 ｜ 枚举型短描述（类型/设备明确且无开放文本）｜ 明确排除项 | 规则通道高置信直接裁决（毫秒级），**不触发 LLM 通道**（或走 7B 轻模型复核）；一致性门禁不变 |
| B 全双通道 | 开放复杂描述（口语化/多意图/模糊） | 规则通道 + LLM 通道（32B）→ 仲裁（秒级，P95 ≤ 5s 🔴） |

> 两条路径的判定结果均须满足：重大强制人工、条款引用合法、等级+条款+置信度输出完备（R-02）；评测集覆盖路径 A/B（Harness 一致性门禁对两条路径同时生效）。

**判级输出**：等级 + 依据条款号 + 条款原文 + 置信度 + 双通道对比（路径 B）；幻觉条款 → 低置信转人工。

**自动判级条件**（全部满足才自动）：双通道一致 ∧ 置信度 ≥ 0.9 ∧ 等级 ≠ 重大；
**强制人工**：任一通道判重大、双通道不一致、低置信——人工确认预填 AI 结论 + 证据链，安全员可改判（改判须填原因，回流评测集）。

**重大隐患组织层级（C1）**：判级复核与销号终裁在**安全主管/分管领导**（安全员仅初核）；复核通过即触发：状态置 ESCALATED → 停产停用通知 + **系统生成书面报告草稿并升级提醒**（A4：一期报送动作线下完成，政务系统对接为二期 TBD-9）。

**自动判级抽检（D3）**：自动判级单按比例（🔴 默认 10%）由安全主管抽检复核，抽检结果回流评测集（Harness）。

**判级响应**：规则初判秒级先出；路径 B 全链路 P95 ≤ 5s（🔴）。

### FR-7 判级可解释与追问

- 判级结果页展示证据链（条款号+原文+置信度+双通道对比）；
- 支持安全员追问「为什么判重大」→ Agent 基于已校验条款生成解释（仅引用条款原文，自由发挥禁止）；
- 解释过程入审计（谁问的、答了什么）。

### FR-8 转整改与路由

- 判级确认后按路由规则生成整改工单：{区域空间节点, 隐患类型, 责任部门映射表（🔴 按组织配置）} → 责任人；
- 整改时限（🔴 按企业制度，默认示例）：一般 15 天 / 较大 7 天 / 重大立即停产整改；
- 重大整改需安全主管确认整改方案后才可开工（方案附件+审批）；
- **特殊作业联动提示（D1）**：整改动作若涉及动火/高处/有限空间等特殊作业，转整改时提示「该整改需办理作业票」（引导至 S2，本期仅提示不联动，二期对接）。

### FR-9 整改上报

- 责任人认领 → 整改 → 上报（照片组 + 说明文字 + 完成时间自述）；
- **照片强制现场拍摄（B3）**：整改照片强制调起相机（禁用相册选择），服务端记录拍摄元数据（时间/设备）；防「拿旧照片充数」——与 FR-10 时间倒挂检测配合；
- 支持**延期申请**（一次，附理由，安全员审批；🔴 制度）；
- 上报后进入「AI 复查初判」（见 FR-10），不直接销号。

### FR-10 AI 复查初判（证据链提供者，不终裁）

- 输入：隐患事件链（上报→判级→整改中→本次上报，来自状态事件表）+ 整改照片组 + 复查照片组 + 说明；
- 证据链生成：时间倒挂检测（整改自述时间 vs 照片时间 vs 状态迁移时间，倒挂即存疑）→ 照片对比初判（前后差异）→ 描述一致性（说装护栏照片里没有=存疑）→ 输出「初判建议：已整改/未整改/存疑 + 证据链」；
- **终裁权在复查员**：通过→CLOSED；不通过→打回 RECTIFYING（附证据链说明缺什么）；AI 永不直接销号；
- 复查照片同样**强制现场拍摄**（B3：调起相机/禁相册/记录元数据）；
- **重大隐患销号（C1/C3）**：销号终裁在安全主管/分管领导（复查员仅现场核验）；销号时记录**报送状态**（待报/已报/备案通过，🔴 按集团监管要求；一期置「待报」并提醒报送，备案动作线下）；
- 重大隐患复查：复查员必须现场复核（App 定位校验在园区内），不可远程销号。

### FR-11 超期督办与升级（阶梯）

| 阶段 | 动作 | 对象 |
|---|---|---|
| 超期 1 天 | Agent 主动对话提醒 | 整改责任人 |
| 超期 3 天 | 提醒 + 抄送 | 安全员 |
| 超期 7 天 | 升级督办工单 | 安全主管 |
| 重大隐患超期 | 立即升级（不受阶梯限制） | 安全主管 + 分管领导 |

### FR-12 全员参与与进度查询

- 上报人随时可查自己的记录与进度（状态 + 当前环节 + 责任人）；
- 匿名上报凭**随机回执号**查询：回执号 ≥10 位随机串（不可枚举，防遍历他人记录），且查询绑定上报手机号二次校验（A3）；不依赖登录；
- 上报人收到状态变更通知（判级结果/已转整改/已销号/打回原因）。

### FR-13 通知与主动对话

- 推送渠道：App 推送为主，短信兜底（🔴 按现有基础设施）；
- Agent 主动对话场景：超期督办、判级被修正（告知原因）、打回说明、重大隐患升级提醒；
- 通知内容合规：不包含敏感字段（作业票号/人员手机号等），脱敏后推送。

### FR-14 统计看板（管理侧，简要）

- 维度：园区/部门/类型/等级/状态/超期；
- 核心指标：上报量、判级一致率（抽检）、闭环率、平均闭环时长、超期率、重大隐患台账；
- 导出：支持安监检查导出（全字段审计口径）。

### FR-15 审计留痕

- 全流程操作（上报/追问记录/抽取修改/判级/确认/转整改/整改/复查/销号/驳回/督办）append-only 记录操作人+时间+变更内容；
- 审计留存 ≥ 1 年（🔴），支持按隐患/按人/按时间导出。

---

## 6. 数据字典（核心字段，与主文档 DDL 对应）

| 实体 | 关键字段 | 说明 |
|---|---|---|
| hidden_danger | danger_id/tenant_id/org_id/dept_id/reporter_id/raw_text/audio_url/photo_urls/source | 隐患主表（11 §3.2）；`source`：随手报/巡检任务/专项检查/告警转化（C4 预留，本期=随手报） |
| 结构化结果 | location(space_node_id+desc)/equipment/danger_type/degree_hint/confidence | FR-4，人工可改，留痕 |
| 判级结果 | level(一般/较大/重大)/level_source(RULE/LLM/HUMAN/ARB)/basis_clause[](clause_id+条款号+原文+版本+clause_type)/confidence/grader_id | FR-6，条款带版本号可追溯；clause_type=法定/企业规则（A2） |
| 工单 | task_id(关联 danger_id)/owner/route_rule_id/due_date/deadline 状态 | FR-8 |
| 整改上报 | rectify_id/photo_urls[]（现场拍摄元数据）/description/claim_finish_time/extension_req | FR-9（B3） |
| 复查 | review_id/初判建议/证据链[]/reviewer_id/final_decision/打回原因/report_status(待报/已报/备案通过) | FR-10（C1/C3） |
| 状态事件 | danger_state_event（append-only） | 责任追溯（11 §3.2） |
| 会话记录 | DRAFT 追问轨迹（问题/回答/耗时） | 评测回流素材（Harness） |

---

## 7. 判级口径与隐患分类词表（v1 初拟，🔴 待我司校准）

### 7.1 三级判级口径（量化判定树，A1 修订）

| 等级 | 判定要点（量化判定树，阈值 🔴 按企业制度） | 处置 |
|---|---|---|
| 重大 | **命中法定判定标准条款**（如《工贸企业重大事故隐患判定标准》行业分册对应情形）；或可能致群死群伤/重大损失 | 停产停用 + 升级上报 + 书面报告草稿；安全主管/分管领导复核（C1） |
| 较大 | 命中**企业规则条目**（制度细则）且满足任一：整改周期 > 30 天（🔴）∨ 需停产/停用部分设备或区域 ∨ 需跨部门资源协调 ∨ 涉及重点区域/特种设备/危化品 | 转整改（7 天，🔴）；安全员确认 |
| 一般 | 以上均不命中：可立即整改或短期（≤ 30 天，🔴）完成、无需停产、单部门可处理 | 转整改（15 天，🔴）；安全员确认 |

**判定顺序（确定性）**：① 法定条款命中 → 重大（强制人工复核）→ ② 企业规则条目匹配（含量化条件）→ 较大 → ③ 其余 → 一般。
> 企业规则条目与法定条款同库同版本管理（§7.3），其规则特征由制度细则配置化生成——保证「较大/一般」判定有可写代码的判据、可标注评测集、可回归（对齐一致性门禁）。

### 7.2 隐患类型词表（12 类，追问选项与抽取约束用）

| # | 类型 | 枚举示例（追问可选项） |
|---|---|---|
| 1 | 消防安全 | 疏散通道占用/消防设施缺失失效/易燃物违规存放/违规动火 |
| 2 | 电气安全 | 配电箱未锁闭/线路私拉乱接/接地缺失/电动设备防护缺失 |
| 3 | 危化品与易燃易爆 | 储存超量/无标识/防护不到位/泄漏迹象 |
| 4 | 机械与设备设施 | 防护罩缺失/安全连锁失效/特种设备超期未检 |
| 5 | 高处与临边 | 临边无护栏/孔洞未覆盖/高处作业无防护 |
| 6 | 有限空间与地下 | 未通风检测进入/无监护 |
| 7 | 动火与热工 | 无票动火/作业票要素不符/监护缺失 |
| 8 | 起重吊装与运输 | 吊具缺陷/区域未隔离/超载 |
| 9 | 劳动防护用品 | 未佩戴/不合格/破损 |
| 10 | 职业健康与卫生 | 粉尘/噪声/高温超标迹象 |
| 11 | 现场管理 | 通道堵塞/5S 混乱/标识缺失/安全出口遮挡 |
| 12 | 其他 | 无法归入以上（追问失败兜底） |

> 🔴 此表为初拟：请按我司实际术语/分类编码增删改后定稿；词表是抽取枚举约束与判级规则通道特征的同源数据（配置化，非硬编码）。

### 7.3 条款库双类型（A2：非重大判级的候选条款来源）

| 类型 | clause_type | 内容 | 用途 | 校验要求 |
|---|---|---|---|---|
| 法定条款 | LEGAL | 法定判定标准（判重大情形） | 重大判级依据 + 引用溯源（合规硬要求） | 条款号/版本必须在库（幻觉 → 低置信） |
| 企业规则条目 | POLICY | 企业制度细则量化规则（整改周期/停产范围/涉重点区域等 → rule_features） | 一般/较大判级依据；较大判定树（§7.1）的特征来源 | 同库同版本、生效区间、变更走评审 |

- 两条目类型同库同版本管理（复用 11 §3.2 `standard_clause` 表，新增 `clause_type` 字段），规则通道特征与词表同源配置化；
- 规则通道候选条款可来自两类；LLM 通道仅可引用规则通道下发的候选条款（FR-6）。

---

## 8. 非功能需求

| 编号 | 项 | 指标（🔴 占位） |
|---|---|---|
| NFR-1 | 结构化+判级响应 | 规则初判秒级；全链路仲裁 P95 ≤ 5s；语音转写 P95 ≤ 10s（60s 音频） |
| NFR-2 | 上传 | 照片 9 张（每张 ≤ 5MB，🔴）弱网可断点续传；语音 ≤ 60s |
| NFR-3 | 可用性 | 上报入口 99.9%；Agent 服务不可用时降级「简单表单直报」（不阻塞一线） |
| NFR-4 | 弱网/断网 | 移动端支持离线草稿（断网可录，联网自动提交） |
| NFR-5 | 安全合规 | 多租户隔离；字段脱敏（照片含人脸/敏感区域处理，🔴）；全量审计；数据不出园 |
| NFR-6 | 一致性 | 判级一致率 ≥95% 纳入发布门禁（Harness，路径 A/B 同时生效）；条款引用合法率 100% |
| NFR-7 | 兼容 | iOS/Android 主流版本（🔴）；H5 兜底（无 App 环境） |
| NFR-8 | 可观测 | 上报漏斗（启动→提交→闭环）埋点；Agent 会话全链路 Trace |
| NFR-9 | 上报人保护（C2） | 上报人身份对整改责任部门/责任人**不可见**（角色级可见性脱敏，独立于 NFR-5 字段脱敏定义）；防止打击报复 |

---

## 9. 异常与容错

| 场景 | 处理 |
|---|---|
| 上报人中途放弃（>10min 无输入） | DRAFT 存草稿 24h，可续填 |
| 追问 5 轮仍有缺口 | 转人工登记（不阻塞）；标记缺字段 |
| 语音转写低置信 | 回显原文确认后再结构化 |
| 判级服务超时/不可用 | 规则通道可用则出「规则初判」+ 标记「AI 判级未完成」转人工；全挂 → 直报人工 |
| 抽取低置信 | 提示人工补录 |
| 重复隐患误判（合并错） | 安全员可拆单（REJECTED 驳回 + 新建） |
| 重大隐患未在时限内书面报告 | 升级提醒主管（不止步） |
| 匿名上报恶意灌水 | 验证码 + 单手机号上报频率限制（🔴 策略） |
| 匿名回执号被尝试遍历 | 回执号 ≥10 位随机 + 手机号二次校验；异常尝试触发限流与告警（A3） |
| 整改/复查照片疑为相册旧图 | 照片强制现场拍摄 + 元数据校验（拍摄时间/设备），无现场拍摄元数据即拒绝提交（B3） |
| 照片无 Exif | 以服务端接收时间为准，复查时间校验标记「时间来源=上传」 |

---

## 10. 验收与测试要点（用例骨架）

| 场景 | 验证点 |
|---|---|
| 多轮追问 | 缺地点→问地点；口语描述「3号仓门口罐子漏」→ 追问补全类型与程度；5 轮上限触发转人工 |
| 结构化回显 | 「我记录为…对吗」逐字段可改；原始描述留档 |
| 判级一致性 | 表述变体组（10+ 说法）判级一致；条款引用合法率 100%（对抗：幻觉条款/过期条款） |
| 重大强制人工 | 任一通道判重大 → 不自动定级 → 人工确认含证据链 |
| 分角色差异 | 员工上报需确认才能转整改；安全员上报直接转 |
| 重复检测 | 同位置同类在办 → 提示合并 |
| 复查不终裁 | AI 初判「已整改」仍需复查员销号；时间倒挂标记存疑 |
| 督办阶梯 | 1/3/7 天提醒对象正确；重大超期立即升级 |
| 审计 | 全链路可导出；改判有原因 |
| Harness 门禁 | 上述判级/抽取用例进 L1/L2 回归；发布拦截 |

---

## 11. 未决问题（TBD，编码前须裁决）

| # | 问题 | 影响 | 状态 |
|---|---|---|---|
| TBD-1 | 隐患类型词表 12 类是否与现行制度一致？是否需要按园区行业分套词表 | 追问选项与抽取枚举 | 待我司校准（§7.2 已留配置化） |
| TBD-2 | 较大判定树的量化阈值（整改周期 > 30 天/停产范围等）与整改时限 | 判级口径与路由 | 判定树模板已入 §7.1，阈值 🔴 按制度填 |
| TBD-3 | 匿名上报是否启用（含短信成本与灌水风险治理） | 全员参与范围 | 建议一期启用 + 频率限制（§9 已含） |
| TBD-4 | 重复隐患命中后：自动合并 or 提示人工选择 | FR-5 流程分支 | 建议提示人工选择（一期） |
| TBD-5 | 照片人脸/敏感信息脱敏策略与成本 | NFR-5 | 待安全评审 |
| TBD-6 | 拍照 VLM 自动识别（本期不做） | FR-2 扩展 | 预留输入接口与字段位 |
| TBD-7 | 推送渠道（App 推送/短信供应商） | FR-13 | 按现有基础设施定 |
| TBD-8 | 上报会话超时(10min)/草稿保留(24h)/自动判级抽检比例等交互参数 | FR-1/3/6 | 默认值已给，待确认 |
| TBD-9 | 重大隐患书面报告：一期线下报送，政务系统对接（报送状态字段已预留） | FR-6/C3 | 二期范围 |

---

## 12. 与相关文档的关系

| 文档 | 关系 |
|---|---|
| 11-场景一-隐患排查治理闭环.md | 本文档的系统设计来源（架构/DDL/Agent 行为/Harness 均引用之）；本文档修订回写 11（状态机分支/词表/source 字段） |
| 20-Agent编排层与Harness工程设计.md | 编排与评测底座约束 |
| 22-私有化部署方案与容量规划.md | ASR/模型服务选型依据 |
| 32-隐患排查上报需求评审意见.md | 本文档 v1.0 的评审来源与修订记录 |
| 10-场景驱动设计总览.md | 业务价值与场景定位 |
