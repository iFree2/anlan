# 场景二：高风险作业审批（动火/有限空间/高处） —— 全链路设计种子素材

> 设计原点：**行业痛点**。八大特殊作业（动火、有限空间、高处、吊装、临时用电、动土、断路、盲板抽堵）是事故高发区，行业壁垒来自：作业票材料多且靠专家审、作业票与现场传感器要联动、审批链责任追溯。
> 使用说明：所有 🔴 为示例占位，使用前必须替换为真实数据/真实经历。

---

## 1. 场景定位与业务价值

### 1.1 行业痛点

1. **作业票材料庞杂**：以动火作业为例，一张作业票含作业方案、风险评估、作业人资质、安全措施（灭火器/监护人/气体检测）、审批链，材料多、勾选框多，审核靠专家人眼找漏洞；
2. **审批是「找漏洞」**：安全员看材料判断「能不能干」，材料不全、逻辑矛盾（作业时间与气体检测时间不匹配、资质过期）靠人工发现——费力且漏检率高；
3. **作业过程失控**：票批了之后作业过程无人盯——动火点附近可燃气体浓度超标还在作业（现实中大量事故源于此）；
4. **责任追溯**：出事故追责，审批链上每个环节的确认记录必须完整可查；
5. **超时失控**：有限空间作业票有有效期，超时未完工需重新检测审批，现实中常被忽略。

### 1.2 Agent 的业务价值

| 价值点 | 说明 |
|---|---|
| 预审提效 | AI 预审材料完整性 + 逻辑一致性，安全员从「全量人眼审」变「复核 AI 结论」，预审时长从小时级到分钟级 |
| 风险提示 | 作业地点/类型/时间交叉分析，主动提示风险点（如临近易燃物、夜间动火） |
| 过程监控 | 作业票 ↔ 现场传感器联动：动火点可燃气体超标自动告警 + 建议中断作业 |
| 责任留痕 | 审批链全量留痕 + 时间戳，追责时可完整还原 |

---

## 2. 业务规则需求规格（Spec，细到可开发）

### 2.1 作业类型与分级（以动火为例，其余类同）

| 等级 | 定义（🔴 按企业制度） | 审批链 |
|---|---|---|
| 一级动火 | 易燃易爆场所、罐区、管廊 | 车间安全员 → 部门主管 → 厂级安全 → 集团备案 |
| 二级动火 | 重点防火区、较大风险区域 | 车间安全员 → 部门主管 → 厂级安全 |
| 三级动火 | 一般区域 | 车间安全员 → 部门主管 |

### 2.2 角色

| 角色 | 职责 |
|---|---|
| 申请人 | 填报作业票（内容/地点/时间/人员/措施/附件） |
| AI 预审引擎 | 材料完整性校验 + 逻辑一致性校验 + 风险提示（不决策） |
| 审批人链 | 按等级逐级审批（可驳回） |
| 监护人 | 作业前现场确认（措施落实）、作业中在岗 |
| 安全员 | 过程监控告警响应、完工确认 |

### 2.3 主流程（状态机）

```
DRAFT(草稿)
  │ 提交
  ▼
AI_REVIEW(AI预审) ──不通过──► REJECTED(驳回，附缺什么/为什么)
  │ 预审通过（或人工复核通过）
  ▼
PENDING_APPROVAL(审批中) ──逐级──► APPROVED(批准)
  │ 监护人现场确认（措施核验）
  ▼
PRE_START(待开工) ──AI联动传感器核验──► EXECUTING(作业中)
  │   （动火点10m内可燃气体浓度达标才允许开工）
  ├── 传感器超标 ──► HALTED(中断) ──复测合格──► EXECUTING
  ├── 票面有效期到期 ──► EXPIRED(失效) ──重新检测审批──► 回到 DRAFT
  ▼
COMPLETED(完工确认) ──签字销票──► CLOSED(已闭环)
  ⚠ 任一审批环节超时 ──► REMINDER(催办)
```

### 2.4 核心业务规则表

| 规则编号 | 规则 | 类型 | 说明 |
|---|---|---|---|
| R-01 | 必填项硬校验 | 强制 | 勾选项/附件/资质有效期任一缺失 → 预审不通过，输出「缺什么」清单 |
| R-02 | 资质校验 | 强制 | 作业人/监护人证书在有效期内、工种与作业类型匹配（规则层，防 LLM 漏检） |
| R-03 | 时间逻辑校验 | 强制 | 三套时钟（见坑#2）：气体检测时间 ∈ [作业开始−30min, 作业开始]；作业时长 ≤ 票面有效期；检测记录与表单时间不矛盾 |
| R-04 | 措施完备性 | 强制 | 按作业类型/等级推导「必备措施模板」（一级动火必须配灭火器+监护人+气体检测），勾选缺失即预审不通过 |
| R-05 | 自由文本合理性 | 建议 | 风险评估描述敷衍/与作业类型不匹配 → AI 标记「存疑」转人工重点复核（LLM 层） |
| R-06 | 传感器开工核验 | 强制 | 开工前自动核验作业点关联传感器实时值（可燃气体 ≤ 报警阈值），不达标禁止开工 |
| R-07 | 过程监控 | 强制 | 作业中周期性复测（🔴 按制度如每 2h）；超标 → 告警 + 建议中断 |
| R-08 | 有效期 | 制度 | 有限空间票 24h、动火票当班有效（🔴 按企业制度）；到期未完工自动失效并提醒重新检测 |
| R-09 | 审批时限 | 制度 | 每级审批限时（🔴 如 2h），超时催办 |
| R-10 | 责任留痕 | 合规 | 审批链每级操作人+时间+意见全量留痕，追责可还原 |

### 2.5 异常分支

| 异常 | 处理 |
|---|---|
| 作业地点无法映射到空间节点/传感器 | 拦截提交，强制从空间树选择地点（见坑#3） |
| 传感器离线 | 开工核验降级为「人工确认检测记录 + 标记传感器离线」，告警提示 |
| AI 预审与人工意见冲突 | 以人工为准，AI 结论与依据留档（可复盘 AI 漏检） |
| 作业中断后复测 | 复测合格才允许恢复，恢复动作记录监护人操作 |

### 2.6 验收标准（节选）

- AC-1：预审必填/资质/时间逻辑三类硬校验拦截率 100%（规则层，不漏）；
- AC-2：自由文本存疑标记准确率 ≥ 85%（LLM 层，🔴）；
- AC-3：传感器开工核验与过程监控告警全量触发，0 漏报（故障注入验证）；
- AC-4：审批链责任留痕 100% 可还原。

### 2.7 Agent 与 Harness 需求补充（Spec 同步）

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| FR-A1 | 填报引导对话 | P0 | Agent 按作业类型引导填报（必填项/附件/资质逐项确认），缺项即时提示 |
| FR-A2 | 预审结果可解释 | P0 | 预审结论输出「硬校验清单（缺什么/为什么）+ 存疑清单 + 风险提示」三部分，支持审批人追问 |
| FR-A3 | 监控主动对话 | P1 | 传感器超标/有效期将到期 → Agent 主动对话（提示中断、复测提醒、重新审批引导） |
| FR-A4 | 审批问答 | P1 | 审批人可向 Agent 提问（材料说明/历史同类票/风险依据），答复引用作业票与规程 |
| FR-A5 | 预审轨迹可回放 | P1 | 预审链全链路录制，Harness 支持 Skill/工具桩化回放与变更注入 |
| FR-A6 | Skill 化封装 | P1 | 预审/开工核验能力封装为 Skill（material_review / start_check），支持灰度发布与回滚 |
| NFR-A1 | 预审响应延迟 | P0 | 硬校验秒级反馈；全链路预审 P95 ≤ 10s（🔴） |
| NFR-A2 | 硬校验拦截率门禁 | P0 | 必填/资质/时间三类规则拦截率 100% 纳入发布门禁（回归防回退） |
| NFR-A3 | 传感器故障注入 | P1 | Harness 支持传感器离线/超标/抖动注入，验证监控 0 漏报 |
| NFR-A4 | 工具调用审计 | P0 | 预审/开工核验/状态推进 Skill 与工具全量审计 |
| NFR-A5 | 工具契约测试 | P1 | query_cert/query_sensor 等核心工具契约测试纳入 CI（不回归） |

---

## 3. 系统设计

### 3.1 总体架构

```
┌────────────────────────────────────────────────────────────────┐
│ 接入层：作业票填报（Web/App/语音）｜ 申请人/审批人/监护人         │
│         （以自然语言与 Agent 交互）                              │
└───────────────────────┬────────────────────────────────────────┘
                        ▼
┌────────────────────────────────────────────────────────────────┐
│ Agent 编排层（作业审批 Agent：LangGraph 编排 + 治理扩展层）      │
│  ├─ 会话入口：填报引导（多轮补全）/ 审批问答 / 监护人确认对话     │
│  ├─ 意图理解：填报 | 预审 | 审批辅助 | 开工确认 | 监控 | 完工     │
│  ├─ 规划与工具调用（ReAct 式多步编排）：                         │
│  │   预审子任务链：材料核验 → 资质核验 → 时间校验 → 措施比对      │
│  │                   → 文本审核（LLM）→ 预审结论汇总             │
│  │   开工核验链：地点解析 → 传感器取数 → 阈值判定 → 放行/拦截    │
│  ├─ 记忆：作业票状态（经工具读）、审批链上下文、会话记忆         │
│  └─ 模型服务：LLM 文本审核 / 风险提示 / 开工语义判定             │
└────────┬───────────────────────────────────────────────────────┘
         ▼ 工具调用（MCP 风格：Schema 校验 + 权限校验 + 调用审计）
┌────────────────────────────────────────────────────────────────┐
│ 业务工具层（Java —— 作业票闭环执行底座，Agent 不可绕过）          │
│  ├─ 作业票引擎：状态机（含中断/失效/恢复）+ 责任留痕             │
│  ├─ 硬校验规则：必填/资质/时间/措施模板（Drools，确定性）        │
│  ├─ 资质台账 / 空间树&传感器映射 / IoT 实时值                   │
│  └─ 审批链服务：分级审批 + 时限催办 + 审计                      │
└────────┬───────────────────────────────────────────────────────┘
         ▼
┌────────────────────────────────────────────────────────────────┐
│ 数据层：作业票库 | 资质库 | 空间树&传感器映射 | 监控事件存储      │
└────────────────────────────────────────────────────────────────┘
```

**Agent 与业务工具层的对接机制（4 条，面试重点）：**

1. **边界原则——预审 Agent 是「智能预审」层，作业票引擎是「闭环权威」层**：Agent 负责填报引导、材料预审编排、文本合理性判断、风险提示；作业票引擎负责状态机、硬校验、审批链、责任留痕。**铁律：Agent 不经由旁路改票状态**——状态推进（提交/驳回/开工/完工）一律走引擎工具（`advance_state`），Agent 无法跳过审批链直接放行。
2. **对接方式——工具化（MCP 风格）**：`submit_permit`、`check_hard_rules`（规则硬校验）、`query_cert`（资质）、`query_sensor`（传感器实时值）、`advance_state`（状态推进）、`confirm_safety`（监护人确认）；工具层统一 Schema 校验 + 权限校验 + 审计。
3. **预审分工——规则在工具层、文本在 Agent 层**：硬校验（必填/资质/时间/措施模板）在业务工具层确定性执行（Drools），LLM 文本审核在 Agent 层（软性、只产生存疑标记）；预审结论由 Agent 汇总呈现为「硬校验清单 + 存疑清单 + 风险提示」，安全员只复核存疑项。
4. **事件回流——监控驱动 Agent 对话**：作业过程传感器超标 → 事件总线 → 作业审批 Agent 主动发起对话（提示中断作业、提醒复测、询问监护人现场情况）；开工核验的「地点→传感器」解析链由 Agent 编排，解析失败经人工确认兜底。

### 3.2 核心数据模型（DDL 节选）

```sql
CREATE TABLE work_permit (
  permit_id      BIGINT PRIMARY KEY,
  work_type      VARCHAR(16) NOT NULL,   -- FIRE / CONFINED / HEIGHT / LIFT ...
  work_level     VARCHAR(16),            -- 一/二/三级
  tenant_id      BIGINT NOT NULL,
  space_node_id  BIGINT NOT NULL,        -- 作业地点（空间树节点，见坑#3）
  applicant_id   BIGINT, applicant_time TIMESTAMPTZ,
  plan_start     TIMESTAMPTZ, plan_end   TIMESTAMPTZ,  -- 计划时间（表单语义）
  actual_start   TIMESTAMPTZ, actual_end TIMESTAMPTZ,  -- 实际时间（系统事件）
  -- 材料
  form_items     JSONB,                  -- 勾选项（措施清单快照）
  attachments    JSONB,                  -- 附件（方案/检测记录/资质）
  risk_assessment TEXT,                  -- 风险评估自由文本
  -- 预审结果
  ai_review      JSONB,                  -- {hard_checks:[...], soft_flags:[...], summary}
  pre_status     VARCHAR(16),            -- PASS / REJECT / FLAGGED
  -- 审批链
  approval_chain JSONB,                  -- [{role, approver_id, decision, comment, ts}]
  state          VARCHAR(32) NOT NULL,
  state_version  INT DEFAULT 0,
  created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
);

CREATE TABLE permit_sensor_binding (      -- 作业票↔传感器绑定（空间映射结果）
  permit_id    BIGINT NOT NULL,
  space_node_id BIGINT NOT NULL,
  sensor_ids   JSONB NOT NULL,           -- 作业点及相邻节点传感器
  bind_mode    VARCHAR(16),              -- AUTO(空间树推导) / MANUAL(人工指定)
  created_at   TIMESTAMPTZ
);

CREATE TABLE permit_monitor_event (       -- 过程监控事件（append-only）
  event_id     BIGINT PRIMARY KEY,
  permit_id    BIGINT NOT NULL,
  event_type   VARCHAR(32),              -- GAS_ALARM / PERIODIC_CHECK / HALT / RESUME
  sensor_id    BIGINT, value  FLOAT, threshold FLOAT,
  action       VARCHAR(32),              -- ALERT / HALT_SUGGEST / AUTO_HALT
  created_at   TIMESTAMPTZ
);
```

### 3.3 预审引擎设计（规则层 + LLM 层，两层分工是核心）

```
规则层（Java/Drools，毫秒级，硬性，R-01/02/03/04）：
  必填项模板：按 work_type × work_level 渲染「必备材料/措施清单」→ 逐项比对
  资质校验：证书库查询（有效期、工种匹配）
  时间逻辑：三套时钟校验（见坑#2 的语义定义）
  措施完备：必备措施模板比对勾选项
  → 任一不通过：REJECT + 输出「缺什么/为什么」清单（转人工也带着清单，提升效率）

LLM 层（Python，秒级，软性，R-05）：
  输入：自由文本（风险评估、作业方案摘要）+ 作业类型/地点上下文
  输出：
    - 文本合理性评分 + 存疑标记（敷衍/与作业类型不匹配/自相矛盾）
    - 风险提示（如「动火点邻近易燃物存放区」「夜间作业」）
  校验：LLM 结论不改变硬校验结果，只产生 FLAGGED 标记 → 人工重点复核

预审结论：PASS（规则过+无存疑）/ FLAGGED（规则过+LLM存疑，人工复核）/ REJECT（规则不过）
```

### 3.4 作业过程监控设计（作业票 ↔ 传感器联动）

```
开工核验（R-06）：permit_sensor_binding.sensor_ids → IoT 实时值 → 可燃气体 > 阈值 → 禁止开工
过程监控（R-07）：定时复测（按制度周期）→ 超标 → 告警（安全员/监护人）+ HALT_SUGGEST
  ├─ 规则可配置：超标即建议中断 / 超标持续 N 分钟自动中断（按企业制度）
  └─ 恢复：复测合格 + 监护人确认 → RESUME（记录操作人）
有效期（R-08）：plan_end 到达未 CLOSED → EXPIRED + 提醒重新检测审批
```

### 3.5 技术选型表

| 组件 | 选型 | 理由（含行业约束） |
|---|---|---|
| 规则引擎 | Drools（Java） | 硬校验确定性、可审计、配置化（各园区制度差异） |
| LLM 文本审核 | Python 服务 + 自研 Prompt | 开放文本合理性判断（软性层） |
| IoT 数据 | 物联中台实时值 API + 本地时序缓存 | 弱网/断连降级 |
| 空间树 | 园区空间树（配置化节点） | 地点标准化是联动前提（坑#3） |
| 状态机 | 自研（Java，事件溯源式） | 责任追溯 + 非法迁移防护 |
| 证书库 | 资质台账系统对接 | 规则层校验数据源 |
| 部署 | 私有化 + 信创适配 | 政企合规 |

### 3.6 作业审批 Agent 行为设计（平台编排层在本场景的落地）

**填报引导 Agent（按作业类型的引导式多轮）**
```
意图=填报 → 按 work_type 加载表单模板（必备项/附件/资质要求来自配置）
逐项引导：内容 → 地点（强制空间树选择）→ 时间 → 作业人资质 → 措施勾选 → 附件
缺项即时提示（规则层秒级反馈），自由文本（风险评估）由 LLM 即时给出「合理性预判」
→ 提交前汇总确认 → submit_permit 工具
```

**预审 Agent（子任务链编排 → material_review Skill，对应 3.3）**
```
意图=预审 → 流程模板（确定性编排）：
  ① 材料快照读取（工具）→ ② 调用 material_review Skill
       （Skill 内部 pipeline：材料核验 → 资质核验 → 时间逻辑校验
        → 措施比对 → LLM 文本审核，逐步校验逐步反馈）
  → ③ 结论汇总（硬校验清单 + 存疑清单 + 风险提示）→ 呈现给申请人/安全员
任一步硬校验不通过 → Skill 终止该链并给出「缺什么/为什么」（可追问，逐项解释）
```

**开工核验 Agent（地点→传感器链 → start_check Skill）**
```
意图=开工确认 → 流程模板：
  ① 地点解析（空间树节点，强制结构化）→ ② 调用 start_check Skill
       （Skill 内部 pipeline：传感器绑定 → 实时值取数（query_sensor）
        → 阈值判定（规则）→ 放行/拦截）
  → ③ 放行 / 拦截（advance_state 工具）
传感器离线 → Skill 降级「人工确认检测记录 + 标记离线」→ 放行带标记（审计可查）
```

**监控对话 Agent（事件驱动主动对话）**
```
传感器超标事件 → 事件总线 → Agent 主动对话：
  提示中断作业（HALT_SUGGEST）→ 询问监护人现场情况 → 复测合格后引导恢复
有效期将到期 → 主动提醒重新检测审批（防 EXPIRED 失控）
```

### 3.7 Harness 应用（预审质量与联动可靠性评测）

```
评测集：
  预审判例集：历史作业票（通过/驳回）+ 人工复核回流（source=human_reviewed）
  + 对抗用例（勾选全选/敷衍文本/时间矛盾，验证坑#1/#2 修复不回退）
L1 Skill 评测（material_review / start_check）：
  绑定用例（硬校验拦截率、存疑标记准确率、开工核验 0 漏报）→ 变更门禁
  Skill pipeline/规则特征/措施模板变更 → 自动触发绑定用例回归
L0 工具契约（query_cert / query_sensor / advance_state）：
  schema / 参数用例 / 错误码契约测试，CI 每日跑
故障注入（联动可靠性）：
  传感器离线 / 超标抖动 / 阈值边界 → 验证开工核验与监控 0 漏报（AC-3）
轨迹回放：
  预审链 Skill/工具桩化回放（READ 真调、WRITE 桩化）→ 变更注入
  （material_review 版本 / 规则特征）→ 拦截率与存疑准确率 diff
回归触发：预审 Skill / Prompt / 规则特征 / 措施模板 / 资质规则任何变更自动触发
```

---

## 4. 实现交底（分模块实现要点）

| 模块 | 实现要点 |
|---|---|
| 动态表单 | 按 work_type 渲染模板（必备项来自配置，各园区制度差异配置化）；勾选快照存 form_items（审批后改表单=版本变更，留痕） |
| 预审引擎 | 规则层先跑（先给用户「秒级硬校验反馈」），LLM 层异步出存疑标记；预审报告 = 硬校验清单 + 存疑清单 + 风险提示，三部分分层展示 |
| 审批链 | 按等级生成审批链；每级意见/时间/操作人留痕；超时催办；驳回附 AI 预审清单（提升驳回沟通效率） |
| 传感器联动 | 空间节点 → 传感器映射表（配置化，含相邻节点推导）；传感器离线降级人工确认 + 标记；监控事件全量入 audit |
| 时间语义 | 统一「计划时间（表单）/实际时间（系统事件）」双语义字段，校验用系统事件时间，展示保留计划时间（见坑#2） |

---

## 5. 实现交底补充：与内嵌支撑能力的关系

| 内嵌能力 | 在本场景的位置 |
|---|---|
| 记忆（任务状态） | 作业票状态机 + 审批链事件（中断/恢复/失效全部可追溯） |
| 知识（制度/规程） | 各作业类型「必备措施模板」由规程知识库配置化驱动 |
| 知识（案例库） | 预审风险提示引用历史事故案例（如同类作业出过事故的地点/条件） |
| 评测（预审判例集） | LLM 存疑标记准确率评测集（人工复核回流标注） |
| 数据闭环 | 审批结果（通过/驳回原因）回流 → 预审规则与 LLM 模板持续优化 |

---

## 6. 踩坑与解法（业务特点驱动的深坑，精而深）

### 坑 #1：LLM 预审「宽松偏差」——材料越多越容易放行，勾选框全选骗过了 AI

**背景**：预审 LLM 层 v1 让 LLM 直接判断「材料是否齐全、措施是否到位」。
**现象**：压测 200 张历史作业票，发现误放行案例高度集中在一个模式：**申请人把所有勾选框全选**（「有防护措施」「已培训」「已检测」全勾），自由文本敷衍（「按标准执行」），LLM 倾向放行——它把「内容多」当「材料全」，把「勾了」当「真做了」。真实场景里工人填表就是这么干的，因为勾选框多且没有校验。
**排查**：分析误放行样本，发现 LLM 无法区分「选项被勾选」与「事实成立」——表单数据本身不携带真实性信息；而敷衍文本（长度短、套话、与作业类型无关）LLM 也会因为「看起来完整」放行。
**解决**：
1. **拆两层，各管各的**：硬校验全部下沉规则层（必填项比对模板、资质查证书库、时间逻辑、措施模板比对）——LLM 不再碰「勾没勾」这类事实判断；
2. LLM 层只做**开放文本合理性**（风险评估敷衍检测、方案与作业类型匹配度），且结论只产生「存疑标记」不产生「通过」；
3. **敷衍检测特征化**：文本长度/信息熵/与模板套话相似度等规则特征 + LLM 判断双通道，命中即 FLAGGED 转人工；
4. 预审报告结构化为「硬校验清单 + 存疑清单」分开展示，安全员只复核存疑项。
**反思**：表单审核场景的教训是——**LLM 擅长「找问题」不擅长「发现问题」**：勾选框全选是行业常态，任何依赖文本「完整性」判断的 AI 都会被骗；事实性校验必须由规则层用权威数据源（证书库、模板）完成，LLM 只配碰开放文本。

### 坑 #2：三套时间「打架」——检测时间、作业时间、审批时间口径混乱，规则引擎拦不住

**背景**：要落地 R-03「气体检测时间必须在动火开始前 30 分钟内」，规则引擎按「表单里的时间字段」比对。
**现象**：上线第一周就发现大量「逻辑矛盾」的作业票没被拦截：检测记录显示 10:00，作业开始填 9:30（检测在作业开始之后）；有的作业时长超过票面有效期一倍。安全员人工复核才发现，规则引擎形同虚设。
**排查**：追查发现「时间」来自三个系统、三种语义：
1. 表单填的「计划作业时间」（申请人随便填）；
2. 气体检测仪的「实际检测时间」（传感器/检测仪记录）；
3. 审批链的「审批时间」（系统事件）。
三者混在一个「时间字段」里比对，规则自然失效；还有跨天、跨时区（集团多园区）问题。
**解决**：
1. **时间语义显式化**：字段区分「计划时间（表单语义，申请人填）」与「实际时间（系统事件语义，系统记录）」，校验只信任系统事件时间（检测记录时间、审批动作时间）；
2. **校验规则重写**：检测时间（系统事件）∈ [实际开工−30min, 实际开工]；实际作业时长 ≤ 票面有效期 + 制度宽限（🔴）；
3. **统一时区**：存储一律 UTC，展示按园区时区；跨天边界测试纳入验收；
4. 规则引擎输出「校验依据明细」（每条规则用哪个时间、哪个来源），人工复核可追溯。
**反思**：业务系统的坑往往不在算法，在**字段语义没定义清楚**——「计划 vs 实际」不分开，再强的规则也拦不住；这个「语义先行」的教训后来成了我做所有校验类需求的原则。

### 坑 #3：作业票和传感器「对不上号」——文字地点到空间点位的联动第一公里

**背景**：做 R-06 传感器开工核验，初版允许作业地点填自由文本（「3号车间东南角」），联动逻辑靠文本解析匹配传感器。
**现象**：联动告警频繁误报/漏报：文本解析「3号车间」匹配到整车间所有传感器，动火点在东南角却被西侧传感器拉高平均浓度误报；有时地点写法不规范（「老车间」「喷漆房隔壁」）直接匹配失败 → 联动缺失，作业票照样开工。
**排查**：本质是**空间语义对齐**问题：文字地点是非结构化的，传感器是结构化点位（设备编号/空间坐标），两者之间没有可靠的映射链路。
**解决**：
1. **地点标准化**：作业地点从「自由文本」改为「园区空间树选择」（园区→车间→区域→点位），表单层强制（坑：存量作业票迁移时兜底映射 + 人工确认）；
2. **传感器映射表配置化**：空间节点 → 传感器列表（含相邻节点推导规则），联动取「作业节点 + 相邻节点」传感器，避免整车间平均；
3. 解析失败兜底：未绑定传感器 → 拦截提交（R 规则），提示人工指定；已开工票标记「未联动」并告警提醒；
4. 绑定关系随空间树变更自动重算 + 变更审计。
**反思**：IoT 联动的第一公里是**空间语义对齐**——把「文字地点」变成「空间树节点」这件事，比浓度算法重要得多；行业里这类「数据标准化」是最大的价值洼地，也最容易被忽略。

---

## 7. JD 对标收口（本场景覆盖的技能点）

| JD 技能点 | 落点 |
|---|---|
| Agent 数据闭环 | 预审→审批→开工→监控→完工全链路 + 驳回/中断原因回流 |
| 任务状态记忆 | 作业票状态机（中断/失效/恢复），断点续办 |
| 上下文工程（MCP Tools） | 传感器实时值工具化接入（开工核验/过程监控） |
| 语义化数据分析（意图/归因） | 自由文本合理性判断、驳回原因聚类分析 |
| 评测（评测集/自动评估） | 预审存疑标记评测集、硬校验拦截率回归 |
| 知识（规程/案例） | 必备措施模板配置化、风险提示引用事故案例 |
| 多租户/权限/审计 | 园区制度差异配置化、审批链责任留痕 |
| Python+SQL/Java | LLM 文本审核 Python、规则引擎/状态机 Java |

---

## 8. 面试素材

### 高频追问
1. 预审为什么拆两层？LLM 被骗的故事（坑#1）→ 事实校验必须规则层 + 权威数据源
2. 时间校验怎么做的？→ 计划/实际双语义 + 系统事件时间 + 统一时区（坑#2）
3. 作业票怎么和传感器联动？→ 空间树标准化 + 映射表 + 相邻节点推导（坑#3）
4. 传感器离线怎么办？→ 降级人工确认 + 标记 + 告警
5. 审批链责任留痕怎么保证？→ 事件溯源式状态机 + 每级操作留痕

### 一句话素材
> 高风险作业审批教会我「AI 与规则的分工」：勾选框全选能骗过 LLM、三套时间能骗过规则——所以事实性校验必须规则层用权威数据源完成，LLM 只配碰开放文本；而作业票与传感器的联动，第一公里是把文字地点变成空间树节点。
