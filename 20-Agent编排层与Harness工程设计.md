# Agent 编排层与 Harness 工程设计（平台级共享底座）

> 本设计是三个场景（S1 隐患判级 Agent / S2 作业审批 Agent / S3 应急指挥 Agent 群）共用的 **Agent 平台底座**：上半部分是「Agent 编排层」怎么设计，下半部分是「Harness 工程」怎么给 Agent 上质量保障。
> 使用说明：所有 🔴 为示例占位，使用前必须替换为真实数据/真实经历。

---

## 1. 定位

```
┌─────────────────────────────────────────────┐
│ 三个行业 Agent（S1 判级 / S2 审批 / S3 应急）  │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│ Agent 编排层（本文档 §2）                     │
│  会话 / 意图 / 规划 / Skill与工具调用 / 记忆   │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│ 能力封装体系（本文档 §2.2）                   │
│  Skill（业务能力单元）＋ MCP Tool（系统接入）  │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│ Harness 工程（本文档 §3）                     │
│  L0 工具契约 / L1 Skill 评测 / L2 Agent 端到端│
└─────────────────────────────────────────────┘
```

- **Agent 编排层**回答「Agent 怎么想、怎么动」：对话、意图、规划、调 Skill/工具、用记忆；
- **能力封装体系**回答「能力怎么沉淀、怎么复用」：Skill 封装业务能力（可评测、可治理），MCP Tool 接入外部系统（原子操作）；
- **Harness 工程**回答「怎么证明 Agent 是对的」：L0 工具契约、L1 Skill 评测、L2 Agent 端到端评测。

---

## 2. Agent 编排层设计

### 2.1 编排内核（LangGraph/LangChain 为主体 + 行业扩展层自研）

> **技术策略**：编排层**以 LangGraph/LangChain 为主体**（图编排 + 工具/MCP 机制），**行业治理层自研扩展**——机制用主流，治理自研，不闭门造车、也不被框架绑架。分工：
> - **LangGraph（Python）**：编排图——确定性流程模板 = 预定义 StateGraph；开放场景 = 动态图/ReAct；
> - **LangChain**：工具抽象（@tool）、MCP 适配（官方 langchain-mcp-adapters）、Prompt/RAG 组件；
> - **自研扩展层**：副作用分级、多租户 ACL、调用审计、工具契约测试、Skill 生命周期治理、逐步骤埋点（挂在 LangGraph 节点回调/checkpoint 上）；
> - **Java 侧**：业务工具层（状态机/规则引擎/审计服务）——AI 编排在 Python（LangGraph 生态），业务闭环在 Java（企业存量系统），MCP/HTTP 解耦。
>
> **为什么不用 Dify/AgentScope**（面试对比）：Dify 低代码对复杂行业流程（判级仲裁、审批链、确定性联动）不灵活、黑盒难扩展；AgentScope 生态相对新、组件少；LangGraph 图编排灵活 + 生态成熟 + 可编程可控，最适合行业强约束。

**① 会话与上下文管理**

```
session 生命周期：CREATED → ACTIVE → SUSPENDED(等待工具/等待人工) → CLOSED
持久化：LangGraph checkpoint（原生支持图状态快照）→ 挂起/恢复
turn 模型：user_msg → 意图 → 规划 → 工具调用(可多步) → 生成 → 记忆写入
上下文窗口预算分配（四段配额）：
  指令(角色+合规红线) | 工具定义(当前相关工具 schema) | 知识/记忆引用 | 对话历史
超预算策略：历史裁剪(按重要性) → 摘要化（复用 S1 压缩思路）
```

- 会话可挂起/恢复（LangGraph checkpoint 官方机制 + 业务侧人工确认），支撑 S2 审批链、S3 确认窗口等长时交互；
- 上下文窗口预算按「四段配额」管理，指令与工具定义段不可裁剪（合规红线不进裁剪池）。

**② 意图理解（三级）**

```
L1 规则：高频固定话术（一键报警、快捷指令）——零成本、确定性
L2 轻量模型：意图分类器（本地小模型，行业意图集：上报/判级/查询/复查/督办...）
L3 LLM：开放场景兜底（复杂长句、多意图混杂、方言口语）
仲裁：L1/L2 命中且置信高 → 直取；否则 L3 + 用户确认回显
```

- 意图集按场景定义（S1：上报/判级/查询/复查/督办；S2：填报/预审/审批/监控/完工；S3：报警/指挥/现场/复盘）；
- L1/L2 保障确定性场景的响应速度与成本，L3 兜底开放表达。

**③ 规划引擎（LangGraph 图编排，天然兼容确定性模板与开放规划）**

```
两种模式在 LangGraph 上实现（图编排两者都原生支持）：

A. 确定性流程模板 = 预定义 StateGraph（业务强约束场景默认）：
   意图 → 预定义状态图（节点 = 步骤，边 = 条件/校验）
   节点类型：规则步骤（Java 服务）/ 工具步骤（MCP）/ LLM 步骤（模型服务）
   例：S2 预审链 = 材料核验 → 资质核验 → 时间校验 → 措施比对 → 文本审核
   失败沿「失败边」终止并给出原因；checkpoint 支持中断恢复

B. 开放场景 = 动态图 / ReAct 循环：
   节点内 LLM 生成计划 → 计划合法性校验（工具存在性/参数 schema/步骤数上限）
   → 逐步执行（循环边）→ 非法计划拒绝 + 重规划（上限 2 次 → 转人工）

行业约束注入（挂在图节点上的扩展层，LLM 永远不能越权）：
   - 合规红线写入系统指令（不可被对话覆盖）
   - 计划步骤数上限、工具白名单、参数 schema 校验
```

- **行业铁律：能模板化就不让 LLM 规划**——判级/审批/应急这类责任动作，流程是确定的，规划自由化是风险源；LangGraph 的预定义 StateGraph 正是「确定性流程」的官方表达，二者天然契合。

**④ 能力封装体系（Skill + MCP 双层，多元化封装）**

```
机制层（LangChain 提供）：
  - 工具抽象：@tool / StructuredTool（注册、参数 schema、结果注入）
  - MCP 集成：langchain-mcp-adapters（官方适配器，加载 MCP 工具进 LangChain）

治理层（自研扩展，框架没有）：
  - 工具注册中心：schema 版本化、灰度、依赖治理
  - 副作用分级（READ/WRITE/BROADCAST）+ 确认窗口语义
  - 多租户 ACL + 调用全量审计
  - 契约测试（L0，防 schema/参数/错误码回归）
  - Skill 生命周期（注册-评测-灰度-发布）+ eval_binding

两层封装：
┌─ Skill（业务能力单元，可复用、可治理、可演进）─────────────┐
│  声明式定义：name/description/input_schema/output_schema/  │
│              pipeline(内部步骤)/few_shot/depends_on/       │
│              eval_binding(评测绑定)/side_effects           │
│  内部实现：编排 MCP 工具 + LLM 步骤 + 规则步骤 的复合能力    │
│  （pipeline 在 LangGraph 上实现为子图/子链）                │
│  示例：danger_grading(隐患判级) / material_review(材料审核)  │
│        plan_match(预案匹配) / evidence_chain(证据链生成)    │
│  生命周期：注册 → 评测 → 灰度 → 发布 → 效果监控 → 版本演进   │
└───────────────────────────────────────────────────────────┘

┌─ MCP Tool（外部系统接入，原子操作）────────────────────────┐
│  注册中心：{tool_name, server_id, input_schema,           │
│            output_schema, side_effect, timeout, retry,    │
│            circuit_break, acl}                             │
│  示例：query_clauses / submit_danger / query_sensor /      │
│        broadcast / unlock_door / pull_video / notify       │
└───────────────────────────────────────────────────────────┘

调用管线（Skill 与 Tool 统一走同一管线）：
  参数 Schema 校验 → 权限校验 → 执行 → 结果注入（截断+结构化）→ 审计
异常处理：超时重试(1次) → 熔断 → 降级（缺能力时给出「人工替代路径」提示）

副作用分级（Skill 与 Tool 都标注）：
   READ    ——可自由调用，回放可真调
   WRITE   ——需上下文校验，回放一律桩化
   BROADCAST ——需确认窗口 + 全量审计，回放强制桩化
```

**Skill 与 MCP Tool 的分工原则（面试重点）：**
- **MCP Tool = 原子操作**：只做「读写外部系统」，无业务语义，不可单独评测「好不好」；
- **Skill = 业务能力单元**：聚合工具 + LLM + 规则，有输入输出契约、few-shot、**绑定评测用例**——Skill 是 Harness 的「可评测单元」，每次 Skill 变更可回归；
- **Agent = 编排**：按意图把 Skill/Tool 组装成流程（流程模板或 LLM 规划），不直接接触外部系统细节；
- **复用粒度**：Skill 供多个 Agent 复用（如「隐患判级 Skill」被上报 Agent、复查 Agent 共用），Tool 被多个 Skill 复用（如 `query_clauses` 被判级 Skill、知识问答 Skill 共用）；
- 副作用分级同时服务 Harness（回放桩化）与安全（高代价动作加锁）。

**⑤ 记忆集成（Memory Kernel）**

```
两类记忆：
  会话记忆（Agent 层）：turn 上下文、意图-动作历史、压缩摘要
  状态记忆（业务工具层）：经工具读取（隐患状态/作业票状态/事件状态），Agent 不直接持有
记忆访问原则：状态以业务工具层为准（读快照），对话上下文以 Agent 层为准
```

- 呼应 S1 坑#2：Agent 判复查必须携带完整事件链——状态记忆经工具注入上下文，不是让 Agent 自己记业务状态。

**⑥ 模型路由（成本-效果联合）**

```
路由维度：意图类型 × 任务难度 × 合规等级
  轻任务（意图分类/实体抽取/简单问答）→ 小模型（本地）
  重任务（判级语义/预审文本/风险分析）→ 大模型
  合规敏感（判级/指令生成）→ 必须大模型 + 规则校验 + 审计
路由依据：评测集分桶结果（Harness 产出）+ 规则优先级（合规敏感强制大模型）
```

- 路由策略由 Harness 评测数据驱动（难度分桶），效果-成本联合报告（呼应 S2 成本优化思路）。

**⑦ 安全边界（四条铁律）**

1. Agent 永不直接改状态（一切状态变更经工具，AI 无终裁权）；
2. 高代价动作（BROADCAST）必须确认窗口 + 全量审计；
3. LLM 输出引用必须可校验（条款号/文档锚点在知识库存在）；
4. 会话与记忆多租户隔离（租户→组织→部门→个人），越权访问拦截即告警。

**⑧ 可观测（全链路 Trace）**

```
埋点：session / turn / intent / plan / tool_call / llm_call / memory_rw / feedback
链路：Kafka → Iceberg → Doris（指标大盘）
用途：Harness 轨迹录制的数据源；故障归因；效果指标（响应时长/成功率/工具准确率/成本）
```

### 2.2 Skill 工程（能力封装与治理）

**Skill 声明式定义（示例：隐患判级 Skill）**

```yaml
name: danger_grading
description: 依据《重大事故隐患判定标准》等对隐患描述判级，输出等级+依据条款
version: v2.1.0
input_schema:
  structured_danger: object      # 结构化抽取结果
  raw_text: string               # 原始描述
output_schema:
  level: enum[一般,较大,重大]
  basis: array[string]           # 依据条款号（必须非空）
  confidence: float
pipeline:                        # Skill 内部管线 = 编排 MCP 工具 + 规则 + LLM
  - step: rule_grading           #   规则通道（query_clauses 工具 + 规则引擎）
  - step: llm_semantic           #   LLM 语义通道（模型服务）
  - step: arbitration            #   双通道仲裁
few_shot:
  - input: {structured_danger: {...}, raw_text: "除尘器没泄爆口"}
    output: {level: "重大", basis: ["工贸判定标准 第X条"], confidence: 0.95}
depends_on: [clause_library]     # 依赖治理（条款库版本）
eval_binding: [case_group_danger_consistency]   # 评测绑定（Harness L1）
side_effects: READ
```

**Skill 运行时（LangGraph 子图 + Java 服务）**

```
pipeline 在 LangGraph 上实现为子图/子链（节点 = 规则步骤 / 工具步骤 / LLM 步骤）
  - 规则步骤（Java 服务：Drools、状态机、审计）经 MCP/HTTP 调用
  - 工具步骤（MCP 工具：query_clauses / query_sensor...）
  - LLM 步骤（模型服务：判级/审核/抽取）
统一执行协议：步骤间传递结构化上下文（schema 校验每步输入输出）
统一评测/审计入口：Skill 执行挂在 LangGraph 节点回调上埋点（复用全链路 Trace）
```

**Skill 生命周期（可治理的关键）**

```
注册（schema 校验）→ 评测（绑定用例跑 Harness L1 回归）
→ 灰度（指定 Agent/场景试用）→ 发布 → 效果持续监控（准确率/采纳率/成本）
→ 版本演进（变更走同一流水线）
变更门禁：Skill 的 pipeline/提示词/few-shot 变更 → 自动触发绑定用例回归
```

**三场景 Skill 清单（示例）**

| 场景 | Skill | 内部步骤（编排的工具/模型） | 副作用 |
|---|---|---|---|
| S1 | danger_grading 隐患判级 | 规则通道 + LLM 语义 + 仲裁 | READ |
| S1 | evidence_chain 复查证据链 | 时间校验 + CV 对比 + LLM 一致性 | READ |
| S2 | material_review 材料预审 | 硬校验规则 + LLM 文本审核 | READ |
| S2 | start_check 开工核验 | 空间解析 + 传感器取数 + 阈值判定 | READ |
| S3 | plan_match 预案匹配 | 事件类型×等级 → 预案检索 | READ |
| S3 | drill_review 复盘分析 | 时间线还原 + 响应评估 + LLM 总结 | READ |

### 2.3 三种 Agent 范式（对应三场景）

| 范式 | 场景 | 编排特征 | 关键设计 |
|---|---|---|---|
| 判定型 Agent | S1 隐患判级 | 双通道仲裁 + 证据链 | 规则通道在工具层、LLM 通道在 Agent 层、条款引用可校验 |
| 流程型 Agent | S2 作业审批 | 子任务链 + 状态机 | 预审链各步工具化、开工核验链、状态推进不可绕过 |
| 协作型 Agent 群 | S3 应急指挥 | 事件驱动多 Agent | 角色分工、事件总线、会话隔离、确定性优先 |

---

## 3. Harness 工程（Agent 的质量保障设施）

### 3.1 定位与分层

```
开发期：单测/集成（用例级）
发布期：回归 + 门禁（评测集级）——每次变更（Prompt/Skill/模型/工具/记忆策略）自动回归
运营期：在线评测 + 演练注入（生产数据回流）——持续发现 Badcase
核心闭环：轨迹 → 评测 → 归因 → 修复 → 回归 → 发布
```

### 3.2 三层评测体系（L0 工具契约 / L1 Skill 评测 / L2 Agent 端到端）

可落地的 Harness 必须**分三层被测对象**——只测 Agent 端到端，Badcase 定位不到 Skill/工具层；只测 Skill，又证明不了 Agent 组合效果：

```
┌─ L0 工具契约测试（MCP Tool 层，CI 每日跑）─────────────┐
│  契约测试套件：每个工具 ① schema 校验 ② 参数用例        │
│  ③ 错误码映射 ④ 边界（超时/空结果/大参数）              │
│  目标：工具契约不回归——参数语义、错误码、schema 稳定     │
│  桩化支持：录制响应桩（回放用）在契约测试中同步校验       │
└────────────────────────────────────────────────────────┘

┌─ L1 Skill 评测（Skill 层，变更即触发）─────────────────┐
│  每个 Skill 绑定评测用例（eval_binding）：              │
│    - 正常用例（各难度分层）                             │
│    - 边界/对抗用例（表述变体、勾选全选、时间矛盾、条款错）│
│  变更门禁：pipeline/提示词/few-shot 变更 → 绑定用例回归  │
│  效果指标：准确率/一致率/拦截率/存疑标记准确率           │
│  可观测：Skill 内部步骤逐段打分（定位到具体步骤）         │
└────────────────────────────────────────────────────────┘

┌─ L2 Agent 端到端（Agent 层，发布门禁）────────────────┐
│  轨迹回放（多 Skill/工具组合的真实轨迹，工具桩化）       │
│  演练注入（脚本化事件流，S3 全要素/断网演练）            │
│  一致性门禁（判级一致率/硬校验拦截率/0 误联动）          │
│  三级评估（规则 + LLM-as-Judge + 人工抽检）            │
│  归因：端到端失败 → 调用链分段打分 → 定位到 Agent 编排   │
│        / Skill 步骤 / 工具契约                         │
└────────────────────────────────────────────────────────┘

Badcase 归因分级（三层联动）：
  端到端失败 → L2 定位到编排层 → 下钻 L1 对应 Skill 评测
  → Skill 失败 → 下钻 L0 工具契约 → 修复回归（自下而上验证）
```

- **L0 是底座**：工具契约不稳，Skill 与 Agent 评测全部失真（呼应 S2 坑#3 协议碎片化）；
- **L1 是性价比核心**：Skill 粒度小、评测快、定位准，大部分 Badcase 在 L1 就能拦住；
- **L2 是最终裁决**：只有端到端通过才能证明「组合后可用」。

### 3.3 核心组件

**① 评测集管理**

```
用例来源分层（source）：
  real      真实轨迹抽样（按场景×难度分层）
  badcase   Badcase 回流（自动/人工确认后入库）
  synth     LLM 合成（基于真实场景模板 + 人工审核）
  labeled   专家标注（双人 + 仲裁，金标准）
用例结构：
  {case_id, scenario, difficulty, trajectory[], golden_answer,
   hard_constraints[], source, tags[], status}
版本化：评测集随资产版本管理，回归用「冻结版本」保证可比
```

**② 轨迹录制与回放（Trajectory Replay）**

```
录制：线上全量 Trace（含工具请求/响应原文）→ Iceberg(raw_trajectory)
回放（离线、无副作用）：
  按 turn 执行，可注入变更：
    - 模型变更（被测模型替换）
    - Prompt/Skill 版本变更
    - 记忆策略变更（衰减参数/压缩策略）
    - 工具行为变更（超时/报错/参数错误——扰动注入，评估鲁棒性）
  工具桩化：按副作用分级——READ 可真调（沙箱只读）；WRITE/BROADCAST 强制录制响应桩
  隔离记忆空间：回放用独立记忆，不污染线上
输出：逐 turn 指标 + 整体 diff 报告（vs baseline）
```

**③ 沙箱评测环境**

```
- 隔离记忆空间（回放不写线上记忆）
- 工具桩 + 只读白名单（写操作强制桩化，BROADCAST 强制桩化 + 计数校验=0）
- 数据隔离（评测租户/虚拟园区数据）
- 发布门禁前置：沙箱内全量回归通过才允许灰度
```

**④ 三级自动评估**

```
L1 规则校验（零成本、硬性）：合规红线、输出 schema、引用存在性、工具参数合法性
L2 LLM-as-Judge（主评估）：多维评分卡（完成度/合规性/工具正确性/引用忠实性）
    - 多裁判投票 + 一致性校验（Kappa）
    - 裁判模型版本冻结 + gold set 校准（防漂移，S1 判级、S2 预审、S3 建议通用）
L3 人工抽检（兜底）：每日抽检 + 校准反馈
```

**⑤ 一致性门禁（行业特色）**

```
- S1 判级一致性：同一隐患表述变体组 → 判级一致率 ≥ 阈值（< 阈值拦截发布）
- S2 硬校验拦截率：必填/资质/时间逻辑 → 规则层 100% 拦截（回归防回退）
- S3 联动正确性：疑似/确定性分级 → 0 误联动（演练验证）
```

**⑥ 演练注入（S3 特色，也用于全平台鲁棒性）**

```
演练脚本：事件流脚本化（时间轴注入：告警→上报→传感器异常→处置动作）
评估项：响应时限（接警→先期处置 ≤ X）、联动成功率、预案匹配率、时间线完整率
断网演练：规则引擎 + 预案缓存本地可用（T0 不依赖网络/LLM）
演练结果回流评测集（source=drill）
```

**⑦ 发布门禁（CI 集成）**

```
变更（Prompt/模型/工具/记忆参数/资产版本）→ 触发：
  沙箱全量回归（冻结评测集）→ 一致性门禁 → 关键指标 diff < 阈值 → 灰度放量 → 全量
不通过 → 拦截 + 归因报告（调用链分段打分，定位到意图/规划/工具/生成环节）
```

**⑧ 数据回流（自进化闭环）**

```
线上 Badcase → 人工确认 → 归因 → 评测集扩充 → 优化（Prompt/规则/路由/记忆参数）
       → 回归验证 → 发布 → 线上指标对比（A/B）→ 效果-成本联合评估
```

### 3.4 三场景 Harness 应用矩阵

| Harness 能力 | S1 判级 Agent | S2 审批 Agent | S3 应急 Agent 群 |
|---|---|---|---|
| L0 工具契约 | query_clauses/submit_danger 契约测试 | query_cert/query_sensor 契约测试 | broadcast/unlock_door 契约测试 |
| L1 Skill 评测 | danger_grading/evidence_chain 绑定用例回归 | material_review/start_check 绑定用例回归 | plan_match/drill_review 绑定用例回归 |
| 评测集 | 判级一致性（表述变体组） | 预审拦截/存疑标记 | 演练事件流脚本 |
| 轨迹回放 | 判级轨迹变更回归 | 预审链 Skill/工具桩回放 | 事件处置全链路回放 |
| 三级评估 | Judge 判级质量抽检 | 文本审核存疑准确率 | 处置建议质量 |
| 一致性门禁 | 判级一致率 ≥95% 🔴 | 硬校验拦截率 100% | 0 误联动 |
| 演练注入 | 隐患上报鲁棒性 | 传感器故障注入 | 全要素演练/断网演练 |
| 数据回流 | 人工修正判级 | 驳回原因/人工复核 | 复盘案例/误报案例 |

---

## 4. 与 JD 对标收口（平台底座覆盖的技能点）

| JD 技能点 | 落点 |
|---|---|
| Agent 开发框架（LangChain/Dify/AgentScope 类） | 编排以 LangGraph/LangChain 为主体 + 行业治理层自研扩展（对比过 Dify/AgentScope） |
| Context Engineering（Prompt/Skills/RAG/Memory/MCP） | 上下文预算、Skill 工程、规划约束注入、工具注册中心、记忆集成 |
| Skills / 智能资产沉淀 | Skill 声明式定义 + 生命周期（注册-评测-灰度-发布）+ 依赖治理 + 评测绑定 |
| MCP / Tool Calling | LangChain @tool + langchain-mcp-adapters 机制 + 自研注册中心/副作用分级/契约测试 |
| 评测体系（评测集/自动评估/Judge/Replay/指标/A-B） | Harness 三层（L0 契约 / L1 Skill / L2 Agent） |
| 语义化数据分析（调用链/归因） | Badcase 归因分级（Agent→Skill→工具）+ 可观测 Trace |
| Agent Memory | 会话记忆（LangGraph checkpoint）+ 状态记忆（Java 业务工具层）分工 |
| Python+SQL / Java | 编排/模型 Python（LangGraph 生态）+ 业务工具层/治理服务 Java |

---

## 5. 面试素材

### 高频追问

**1. 编排层为什么以 LangGraph/LangChain 为主体，而不是全自研？**（面试必问，展开版）

> 我的技术策略是「**机制用主流，治理自研**」——编排层以 LangGraph/LangChain 为主体，行业治理层自研扩展。选择 LangGraph 的出发点：
>
> **① 图编排天然兼容「确定性优先」**：LangGraph 本质是状态图——**确定性流程模板 = 预定义 StateGraph**（节点=步骤、边=条件校验），开放场景 = 动态图/ReAct。判级双通道仲裁、预审链、应急联动这些责任动作，用预定义图就是官方表达；不用像全自研那样再造一套流程引擎。
>
> **② 工具/MCP 机制成熟**：LangChain 的 @tool 注册、官方 langchain-mcp-adapters 直接可用，不用重复造轮子。
>
> **③ checkpoint 原生支持长时交互**：会话挂起/恢复（等人工确认、等工具返回）用 LangGraph checkpoint，天然支撑审批链、确认窗口。
>
> **④ 生态成熟**：社区大、招聘成本低、踩坑资料多，团队协作与演进有保障。
>
> **⑤ 对技术应用有自己的思考——自研收敛到「行业治理层」**：框架有机制、没有治理。自研的是框架给不了的：副作用分级（READ/WRITE/BROADCAST）、多租户 ACL、调用全量审计、工具契约测试、Skill 生命周期（注册-评测-灰度-发布）、逐步骤埋点（挂在 LangGraph 节点回调/checkpoint 上）。这层才是平台的差异化，也是「治理层自研、机制层主流」的真正含义——不是框架不行，是机制之上的治理没人替你做。
>
> **对比过 Dify / AgentScope**：Dify 低代码对复杂行业流程不灵活、黑盒难扩展；AgentScope 生态相对新、组件少；LangGraph 图编排 + 可编程可控最适合行业强约束。

2. 上下文预算怎么分？→ 四段配额，指令与工具/Skill 定义段不可裁剪
3. Skill 和 MCP 工具什么区别？→ Skill=业务能力单元（聚合工具+LLM+规则，可评测可治理）；MCP Tool=原子接入（无业务语义）；Agent 编排组合两者
4. Skill 变更怎么保证不回退？→ eval_binding 绑定用例 → L1 回归 → 灰度 → 发布，变更门禁
5. Harness 为什么分三层？→ L0 工具契约（底座稳）/ L1 Skill 评测（性价比核心，定位准）/ L2 Agent 端到端（最终裁决）；Badcase 归因三级下钻
6. 回放怎么保证无副作用？→ 副作用分级：READ 真调、WRITE/BROADCAST 强制桩化
7. 发布门禁拦了什么？→ L0 契约 + L1 Skill 回归 + L2 一致性/拦截率/误联动
8. LLM-as-Judge 怎么防漂移？→ 裁判版本冻结 + gold set 校准 + Kappa
9. LangChain 有 Memory 模块为什么不够？→ 会话级短记忆框架有，但任务状态记忆（状态机+事件溯源）、多租户隔离、合规审计要自研（状态记忆在 Java 业务工具层，经工具读）
10. 编排层用 Python（LangGraph），Java 的价值在哪？→ AI 编排在 Python 生态（LangGraph 成熟），业务闭环在 Java（状态机/规则引擎/审计/存量系统），MCP/HTTP 解耦——这是主流「AI 应用 + 企业系统」架构；我负责整体架构与治理层设计

### 一句话素材
> 我把 Agent 平台拆成三块：编排层以 LangGraph/LangChain 为主体（图编排 + 工具/MCP 机制），能力封装层让能力「可沉淀、可复用」（Skill 管业务能力、MCP 管系统接入），Harness 让平台「证明它对」（L0 工具契约、L1 Skill 评测、L2 Agent 端到端三层把关）。自研收敛到框架给不了的「行业治理层」——副作用分级、多租户审计、Skill 生命周期、逐步骤埋点。机制用主流、治理自研，这就是我对技术应用的理解。
