# 安澜平台技术选型审视与升级（架构师视角）

> 本设计从**系统架构师视角**对「安澜」平台现有技术选型做一次体检：哪些合理（保留）、哪些欠合理（调整）、哪些可引入更主流方案（升级）。
> 评估框架：**行业约束适配度（真实性）× 技术先进性（逼格）× 落地可行性（能交付）**——三者取交集，不为新技术而新技术，每个引入都必须有场景落点。
> 使用说明：🔴 为占位；「最终落地」表示可当主导经历讲，「选型对比过」表示可当评估经历讲（真实且逼格不减）。

---

## 1. 审视结论总览

| 层 | 当前选型 | 评估 | 结论 |
|---|---|---|---|
| Agent 编排 | 自研 Java Runtime | ⚠️ 欠主流 | 升级为 LangGraph/LangChain 为主 + 行业治理层自研 |
| 意图/入口 | 文本/语音 + 三级意图 | ⚠️ 缺多模态 | **引入 VLM 拍照识隐患**（海康视觉基因，最大差异化） |
| RAG/知识 | Milvus + ES + Neo4j + bge 系 | ✅ 主流 | 保留 + 升级 GraphRAG + 语义切分 + 新向量模型 |
| 规则引擎 | Drools | ✅ 合理 | 保留（制度差异配置化是真实刚需） |
| 状态机 | 自研事件溯源 | ✅ 合理 | 保留（对比 BPMN 的取舍要能讲） |
| 记忆 | Redis + PG + Milvus | ✅ 合理 | 保留 + 遗忘曲线工程化 + 对标 Mem0/Zep |
| 模型推理 | 私有化大模型 | ⚠️ 缺工程化 | **引入 vLLM/SGLang + 量化 + 国产卡适配 + LLM Gateway** |
| 数据管道 | OTel + Kafka → Iceberg → Doris | ✅ 主流 | 保留 + 补 Flink 实时告警/特征 |
| LLMOps | 自研 Trace | ⚠️ 缺对标 | **引入 Langfuse 式 LLMOps**（对标 LangSmith/Langfuse） |
| Harness | 自研三层评测 | ✅ 方向对 | 保留 + 对标 DeepEval/Ragas/Promptfoo 能力 |
| 语音 | ASR（未指定） | ⚠️ 待定 | 引入 FunASR/Whisper 私有化 |
| 部署 | 私有化 + 信创 | ✅ 合理 | 保留 + K8s + GPU 调度明确化 |

**一句话结论**：底座方向（规则兜底 + MCP/Skill + 私有化）是真实的行业答案，保留；**编排层从「自研为主」调整为「LangGraph/LangChain 为主 + 行业治理层自研」**（更主流、不封闭、体现技术应用思考）；升级点集中在「多模态入口、推理工程化、LLMOps、RAG 增强」四个方向——这些是 2024-2025 的主流技术水位，补上后平台从「业务正确」升到「技术主流」。

---

## 2. 关键审视点与升级建议

### 2.1 Agent 编排：自研核心 + 对标主流框架（Spring AI / LangGraph）

**现状**：自研 Java Runtime（会话/意图/规划/Skill 调用）。
**审视**：⚠️ 欠主流——编排层应拥抱 LangGraph/LangChain 生态，理由要精确：**不是「框架不支持」，而是「框架有通用机制（@tool 注册、langchain-mcp-adapters MCP 集成、LangGraph 状态图），缺的是行业治理语义」**：副作用分级、多租户 ACL、调用审计、契约测试、Skill 生命周期治理是注册机制之上的治理层，框架不给。策略：**机制用主流（LangGraph/LangChain 编排 + 工具/MCP），治理自研（行业扩展层）**——不闭门造车，也不被框架绑架；同时能回答「为什么不用 Dify/AgentScope」。

**升级方案（最终落地）**：
```
技术策略：机制用主流、治理自研
  编排：LangGraph（Python）为主体——确定性流程模板 = 预定义 StateGraph，
        开放场景 = 动态图/ReAct；checkpoint 支持长时交互挂起/恢复
  工具/MCP：LangChain @tool + 官方 langchain-mcp-adapters
  自研扩展（行业治理层）：副作用分级、多租户 ACL、调用审计、
        工具契约测试、Skill 生命周期（注册-评测-灰度-发布）、逐步骤埋点
  业务闭环：Java（状态机/规则引擎/审计服务），经 MCP/HTTP 与编排层解耦
S3 多 Agent 协作：LangGraph 多 Agent 模式（Supervisor/状态共享）——
  事件识别 → 先期处置 → 决策增强 为并行分支（状态共享 + 条件迁移）
```

**逼格点**：能讲清「机制用主流、治理自研」的取舍——LangGraph 图编排天然兼容确定性流程模板，不是自嗨造轮子；自研收敛到框架没有的行业治理层。
**真实性**：编排层 Python（LangGraph 生态）+ 业务层 Java（企业存量系统）是 2024-2025 主流「AI 应用 + 企业系统」架构；Java 团队做治理层设计与架构，分工真实。

### 2.2 多模态入口：VLM 拍照识隐患（海康视觉基因，最大差异化亮点）

**现状**：只支持文本/语音上报。
**审视**：浪费了公司最大的资产——**视觉**。一线排查员最自然的动作是「拍照」，隐患照片（未戴安全帽、通道堵塞、灭火器缺失、护栏破损）本身就是最强的结构化信号。

**升级方案（最终落地）**：
```
新增「拍照识隐患」入口：
  VLM（视觉语言模型，私有化部署）识别照片：
    - 隐患对象检测（安全帽/灭火器/护栏/通道占用/危化品存放）
    - 隐患描述生成（「3号仓灭火器缺失，箱门敞开」）
    - 与文本描述交叉融合（照片 + 语音/文字双通道 → 结构化抽取）
  与判级链衔接：VLM 输出直接进结构化抽取 → danger_grading Skill
  复查场景复用：整改前后照片对比由 VLM 完成「是否已整改」初判
  模型：Qwen2.5-VL 系（7B 私有化可跑）/ 自研检测模型（公司视频 AI 能力）
```

**逼格点**：多模态 Agent 是 2025 主流方向；「海康背景 + 视觉 + Agent」三者结合是别人抄不走的差异化叙事。
**真实性**：海康的视觉/视频 AI 能力是真实存在的平台资产，「复用公司视频 AI 能力接入 Agent」非常合理。
**注意**：🔴 如果你真实项目没有做视觉，这段按「选型评估过 + 规划中」表述，或弱化为「照片辅助核查（已有 CV 对比）」现有内容。

### 2.3 RAG 增强：GraphRAG + 语义切分 + 新一代向量模型

**现状**：Milvus + ES BM25 + Neo4j + bge-large-zh + bge-reranker。
**审视**：混合检索 + RRF + Rerank 已是标配（方向正确）；可升级的三点：
1. **向量模型换代**：bge-large-zh → **bge-m3**（多语言 + 多粒度 + 长文档）或 Qwen3-Embedding（中文更强）；
2. **GraphRAG**（微软开源方法论）：条款-案例-区域的关系不是简单向量能表达的——「同一类隐患在不同园区的判级口径」「某条款关联的既往案例」，GraphRAG 的社区检测 + 层级摘要对安全生产知识特别契合；
3. **语义切分**：法规按「章-节-条」层级切分之上，规程/案例用语义切分（按语义完整块而非固定长度）。

**升级方案（最终落地）**：
```
检索管线：Query 理解 → 向量(bge-m3) + BM25 + 图(GraphRAG 社区检索)
        → RRF 融合 → bge-reranker 精排 → 引用锚点
GraphRAG 落点：条款库 + 案例库 + 园区/区域关系 → 社区检测
  「该条款在哪些园区适用」「同类隐患的历史判级分布」→ 判级一致性参考
```

**逼格点**：GraphRAG 是 2024 下半年-2025 的检索热点；把「向量 + 图 + 社区」讲清楚就赢了大多数候选者。
**真实性**：Neo4j 已有（图底座现成），GraphRAG 是增量升级，逻辑自洽。

### 2.4 模型推理工程化：vLLM/SGLang + 量化 + 国产卡 + LLM Gateway

**现状**：私有化大模型 + 难度分桶路由 + 语义缓存（未指定推理引擎）。
**审视**：行业落地的痛点从来不是「选哪个模型」，而是「私有化怎么跑得快、跑得省、国产环境跑得起来」。缺推理工程化 = 面试被问「vLLM 和原生推理差多少」答不上来。

**升级方案（最终落地）**：
```
推理引擎：vLLM（PagedAttention，吞吐主流）/ SGLang（复杂 prompt 场景）
量化：AWQ / GPTQ / FP8（单卡可跑 7B-32B）
国产卡适配：昇腾 CANN / 寒武纪（信创硬要求，选型对比过）
LLM Gateway（自建，对标 LiteLLM）：
  统一模型接入（多模型 + 多实例 + 版本）
  路由（难度分桶）→ 限流/降级 → 语义缓存（GPTCache 式，向量命中复用）
  成本核算（token 计量入 Doris，联动 Harness 效果-成本报告）
模型族：Qwen 系（私有化主流，7B/14B/32B 按任务分档）
```

**逼格点**：vLLM + 量化 + 国产卡 + Gateway 四件套是「真做过私有化 LLM 平台」的标志，绝大多数 Agent 应用开发者没这层经验。
**真实性**：信创环境（昇腾/寒武纪）是你司真实约束，「选型对比过」完全可信。

### 2.5 LLMOps：Langfuse 式全链路可观测

**现状**：OTel 全链路 Trace（Kafka → Iceberg → Doris）。
**审视**：方向正确，但缺「LLM 专属观测维度」——token 消耗、prompt 版本、模型响应质量、单次生成延迟，这些是 LLM 应用特有的，通用 Trace 覆盖不全。

**升级方案（最终落地）**：
```
自建 LLMOps（对标 Langfuse / LangSmith，私有化部署）：
  Trace：session/turn/tool_call/llm_call（OTel 已有）+ prompt 版本 + token 计量
  Span 增强：LLM 调用的输入输出采样（合规：敏感字段脱敏后采样）
  面板：单会话时间线回放（复现 Badcase 的 UI）、token 成本分布、模型延迟分位
  与 Harness 打通：Trajectory Replay 直接消费 LLMOps 录制的轨迹
```

**逼格点**：Langfuse/LangSmith 是 LLMOps 主流词，讲「私有化自建对标方案」既懂行又符合数据不出园约束。
**真实性**：Langfuse 开源可自建，真实团队常这么做。

### 2.6 Harness 对标主流评测框架（DeepEval / Ragas / Promptfoo）

**现状**：自研三层评测（L0 契约 / L1 Skill / L2 Agent）。
**审视**：方向正确，补充「对标了什么」能让逼格更实——Harness 不是拍脑袋，是吸收了主流评测框架的能力。

**升级方案（选型对比过 + 部分落地）**：
```
能力对标：
  Ragas（RAG 评测）：faithfulness / answer_relevancy / context_precision
    → 映射到我们的「引用忠实度 / 检索命中率」评测维度
  DeepEval（LLM 应用评测）：断言式评测 + LLM-as-Judge 模式
    → 映射到 L1 Skill 绑定用例
  Promptfoo（Prompt 回归）：prompt 变更自动回归对比
    → 映射到 Skill 变更门禁
结论：对标能力自研（私有化约束下，评测框架必须本地跑、可审计、接条款库）
```

**逼格点**：能说出 Ragas 的 faithfulness 和我们「引用一致性校验」的关系——评测体系是「懂行地自研」而非「闭门造车」。

### 2.7 实时链路：补 Flink（告警流 / 实时特征）

**现状**：Kafka → Iceberg → Doris（批式指标为主）。
**审视**：S3 应急、S2 传感器监控有实时性要求（告警、联动），批式管道满足不了毫秒-秒级实时路径。

**升级方案（最终落地）**：
```
Flink 实时链路：Kafka 事件流 → Flink（告警规则 / 实时特征 / 事件时间线融合）
  - S2：传感器实时值滑动窗口 → 超标判定 → 告警/中断建议（秒级）
  - S3：多源事件（告警/上报/动作）实时融合入事件时间线
  - 指标：实时大盘（今日联动次数/告警量）与 Doris 批式互补
```

**逼格点**：实时链路（Flink）是数据平台经验的硬通货；补上后数据架构「实时 + 批式」完整。
**真实性**：你 8 年应用开发对接过 IoT 告警链路，Flink 引入自然。

### 2.8 工作流取舍：状态机 + 事件溯源 vs BPMN（Camunda）

**现状**：自研状态机 + 事件溯源。
**审视**：✅ 这是**正确选择**，但要能讲清「为什么不用 Camunda/Flowable」：
- 我们的状态机是「轻量、强约束、可审计」：隐患/作业票/事件状态迁移简单（5-8 个状态），BPMN 的泳道/网关/人工任务编排是重型武器；
- 事件溯源满足责任追溯（append-only），Camunda 历史表是另一套语义；
- 保持自研，面试话术：**「对比过 Camunda 8（其有 Agent 化趋势），但我们的场景状态少、约束强、要私有化轻量部署，自研状态机 + 事件溯源更贴合」**。

**逼格点**：能主动讲「为什么不用 Camunda」= 做过选型思考的架构师。

### 2.9 记忆工程：遗忘曲线 + 分层（对标 Mem0/Zep 工程实践）

**现状**：短期 Redis + 长期 PG/Milvus + 时间衰减。
**审视**：方向对；补充两个主流工程点：
1. **遗忘曲线工程化**：时间衰减从「指数函数」升级为「Ebbinghaus 遗忘曲线分段」（近期高频衰减快、长期低频衰减慢的分段函数），贴近真实记忆规律；
2. **对标 Mem0/Zep**：选型对比过 Mem0（OpenMemory 的 add/search 抽象）、Zep（Graphiti 时序图记忆），结论：行业多租户隔离 + 事件溯源自研（前面已有），记忆抽象层参考 Mem0 的 API 设计。

**逼格点**：遗忘曲线 + Mem0/Zep 对标 = 记忆体系「懂行」的证据。

### 2.10 语音与边缘

```
ASR：FunASR（阿里开源，私有化，中文方言好）或 Whisper 系 → 已有 ASR 置信度回显设计复用
边缘（S3 弱网）：端侧小模型（Qwen-1.5B 类）+ 本地规则引擎 + 预案缓存（已有）
  → 明确「边缘-中心」两级：边缘做确定性动作（T0），中心做增强（T0+）
```

---

## 3. 升级后分层技术栈总表（面试可整体背）

| 层 | 技术选型（升级后） | 关键理由 |
|---|---|---|
| 编排层 | LangGraph/LangChain 为主 + 行业治理层自研（Python 编排 + Java 业务工具层） | 图编排兼容确定性模板；治理层自研（副作用/审计/Skill 生命周期） |
| 多模态 | VLM 拍照识隐患（Qwen2.5-VL 私有化）🔴 | 视觉基因差异化 |
| Skill/工具 | Skill 工程 + MCP 注册中心（副作用分级） | 可评测单元 + 系统接入 |
| 模型服务 | Qwen 系（7B/14B/32B 分档）+ vLLM/SGLang + AWQ 量化 + 国产卡适配 | 私有化推理工程化 |
| 路由/缓存 | LLM Gateway（对标 LiteLLM）+ 语义缓存（GPTCache 式） | 成本-效果联合 |
| RAG | Milvus + ES + Neo4j（GraphRAG）+ bge-m3/bge-reranker | 混合检索 + 社区检索 |
| 记忆 | Redis + PG + Milvus + 遗忘曲线分段衰减 | 对标 Mem0/Zep 工程 |
| 规则/状态 | Drools + 自研状态机（事件溯源） | 制度配置化 + 责任追溯 |
| 数据 | OTel + Kafka + Flink（实时）+ Iceberg + Doris | 实时 + 批式双链路 |
| LLMOps | 自研（对标 Langfuse/LangSmith） | 轨迹可观测 + Badcase 复现 |
| Harness | 自研三层（对标 Ragas/DeepEval/Promptfoo） | 契约 + Skill + 端到端 |
| 部署 | 私有化 + 信创（昇腾/寒武纪适配）+ K8s GPU 调度 | 政企合规 |

---

## 4. 逼格 vs 真实性的边界（红线）

| 表述等级 | 适用 | 示例 |
|---|---|---|
| 「最终落地」 | 你真实做过 | LangGraph 编排、Drools、状态机、混合检索、私有化部署 |
| 「选型对比过」 | 你真实调研评估过 | Spring AI/LangChain4j、Mem0/Zep、Camunda、vLLM vs SGLang、GraphRAG |
| 「评估 + 规划中」 | 真实方向但未落地 | VLM 拍照识隐患（🔴 按真实）、国产卡适配细节 |

**红线**：面试官（尤其阿里云这类平台厂）对技术选型极敏感——
1. vLLM/量化/GraphRAG 这类词**必须能讲出原理和取舍**，背名词必崩；
2. 「选型对比过」必须能说出对比维度（吞吐/延迟/量化损失/社区活跃度/国产卡支持）；
3. 不要堆砌：每个引入技术都要能接「为什么在这里用、不用会怎样」。

---

## 5. 面试话术（如何回应「你是不是在堆技术」）

> 我的选型原则是三件事的交集：**行业约束（私有化/信创/合规/弱网）决定底线，业务场景决定落点，主流技术决定水位**。
> 底线层面：数据不出园所以私有化模型 + 自研推理工程；合规所以要规则兜底、引用可校验、全量审计。
> 落点层面：每个技术都长在场景上——VLM 是因为排查员最自然的动作是拍照，GraphRAG 是因为条款-案例-园区的关系不是向量能表达的，Flink 是因为告警有秒级实时要求。
> 水位层面：编排抽象对标 Spring AI、评测对标 Ragas/DeepEval、可观测对标 Langfuse——不是闭门造车，是懂行的自研。
> 我只选「讲得清原理、答得了取舍」的技术，背名词的技术不进入我的方案。

---

## 6. 一句话素材

> 这套平台的技术底色是「行业约束选底线、业务场景定落点、主流技术提水位」：私有化 + 信创 + 规则兜底是底线，VLM 拍照识隐患、GraphRAG、Flink 实时链路是落点，vLLM 推理工程化、LLMOps、三层 Harness 是对标主流的水位——三者取交集，才是既真实又有逼格的方案。
