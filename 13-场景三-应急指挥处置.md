# 场景三：应急指挥处置 —— 全链路设计种子素材

> 设计原点：**行业痛点**。突发事件处置是安全生产的「最后一道防线」，行业壁垒来自：黄金处置时间秒级、预案是几百页 Word、多系统联动（广播/门禁/视频）且误触发代价极大。
> 使用说明：所有 🔴 为示例占位，使用前必须替换为真实数据/真实经历。

---

## 1. 场景定位与业务价值

### 1.1 行业痛点

1. **黄金时间秒级**：火灾、危化品泄漏、人员伤害，先期处置（通知、疏散、隔离、报警）必须秒级——等专业力量到达前这几分钟决定后果；
2. **预案是几百页 Word**：事发时靠人翻文档，找不到对应章节，动作顺序记不全；
3. **多系统联动割裂**：广播（疏散通知）、门禁（开疏散门）、视频（调现场画面）、短信/App（通知责任人）、物资，各系统独立，人工逐个操作；
4. **误触发代价大**：全园区广播疏散一次的成本极高，误报驱动的联动会摧毁信任（狼来了效应）；
5. **复盘靠回忆**：处置过程（谁、何时、做了什么）没有完整时间线，复盘和追责困难。

### 1.2 Agent 的业务价值

| 价值点 | 说明 |
|---|---|
| 秒级先期处置 | 规则引擎毫秒级触发确定性动作（通知/广播/门禁/视频），LLM 并行生成增强建议 |
| 预案即用 | 几百页 Word → 结构化预案库 → 事件类型/等级即时匹配，动作清单逐条下发 |
| 一屏指挥 | 事件时间线 + 现场视频 + 任务清单 + 资源状态一屏聚合 |
| 完整复盘 | 多源事件自动融合成处置时间线，复盘与追责有据可依 |

---

## 2. 业务规则需求规格（Spec，细到可开发）

### 2.1 事件类型与分级

| 事件类型 | 示例 | 等级（🔴 按企业制度） |
|---|---|---|
| 火灾 | 车间起火、电气火灾 | 一般（班组可处置）/ 较大（车间级）/ 重大（厂级/上报） |
| 危化品泄漏 | 气体/液体泄漏 | 同上 |
| 人员伤害 | 机械伤害、触电、坠落 | 同上 |
| 设备事故 | 压力容器异常、燃气泄漏 | 同上 |

**分级依据**：伤亡/财产/影响范围/失控趋势（规则 + 人工确认，重大事件自动升级上报）。

### 2.2 角色

| 角色 | 职责 |
|---|---|
| 报警人 | 一键报警/电话/App 上报（含照片/语音） |
| 值班员 | 确认事件、执行先期处置、调度 |
| 应急指挥组 | 现场指挥、任务分派 |
| 现场处置人员 | 执行任务（灭火/堵漏/疏散引导）、上报处置进展 |
| 系统 | 自动联动（广播/门禁/视频/通知）+ 事件时间线记录 |

### 2.3 主流程（两级响应，见坑#1）

```
事件发现（多源）：
  人工上报（一键报警/电话）｜ 系统告警（烟雾/气体/视频AI事件）

事件识别与分级（规则 + 快速模型，毫秒级）：
  ├─ 确定性告警（手动报警/气体浓度超标）──► 直接触发联动
  └─ 疑似告警（视频AI事件：烟雾/聚集/摔倒）─► 人工确认窗口(N秒) ──确认──► 触发联动
                                                    └─未确认──► 仅记录，不联动（见坑#3）

T0 先期处置（规则引擎，毫秒级，确定性动作）：
  通知（值班/安全主管/责任人名单）
  广播（按事件地点匹配疏散区域话术）
  门禁（疏散门解锁）
  视频（调取事件地点附近摄像头画面）
  预案匹配（事件类型×等级 → 结构化预案加载到指挥台）

T0+ 增强与指挥（LLM 并行，秒级，辅助）：
  风险分析与处置建议（依据预案 + 实时传感器数据）
  任务清单生成（动作→责任人→时限）
  持续事件时间线融合（上报/动作/传感器/视频快照）

处置与闭环：
  任务执行 → 现场上报进展（语音/文字/照片）→ 时间线更新
  处置完成 → 复盘：时间线还原 + 响应时限评估 + 处置评估
```

### 2.4 核心业务规则表

| 规则编号 | 规则 | 类型 | 说明 |
|---|---|---|---|
| R-01 | 两级响应 | 架构 | 确定性动作规则引擎先行（毫秒级），LLM 增强并行（秒级），LLM 不阻塞联动 |
| R-02 | 联动分级 | 强制 | 确定性告警直接联动；疑似告警人工确认窗口后才联动（防误触发） |
| R-03 | 通知名单 | 制度 | 事件类型×等级 → 通知对象清单（值班/安全主管/厂长/必要时 119/120），名单配置化 |
| R-04 | 疏散规则 | 制度 | 事件地点 → 疏散区域/路线/集合点/清点动作，由空间树 + 预案推导 |
| R-05 | 联动动作 | 制度 | 火灾 → 广播疏散+门禁解锁+视频调取+排烟联动；泄漏 → 广播疏散+隔离警戒+通风；按预案配置 |
| R-06 | 响应时限 | 制度 | 接警→先期处置动作 ≤ X 分钟（🔴 按企业制度，如 3 分钟），时限进复盘评估 |
| R-07 | 预案结构化 | 强制 | 预案必须结构化入库（事件→等级→动作清单→责任人→资源），纯文本不可用于联动 |
| R-08 | 时间线 | 强制 | 多源事件（告警/上报/动作/传感器）按统一时间轴融合，缺时间戳事件标记存疑 |
| R-09 | 广播撤回 | 强制 | 广播类联动支持「撤回/更正」动作（误触发的补救，见坑#3） |
| R-10 | 复盘留痕 | 合规 | 处置全过程（决策、动作、上报）全量留痕，复盘报告自动生成 |

### 2.5 异常分支

| 异常 | 处理 |
|---|---|
| 事件地点解析失败 | 联动降级：按「全园区广播」不执行，改为通知值班员人工确认地点（防误播全园） |
| 联动系统故障（广播/门禁） | 失败动作记录 + 人工替代路径提示（对讲/人工开门），联动成功率入复盘 |
| 视频 AI 高误报时段（雨雾/施工烟尘） | 疑似告警确认窗口动态调整 + 时段规则降权 |
| 断网（私有化弱网环境） | 预案库本地缓存 + 规则引擎本地可跑（毫秒级联动不依赖云），事件离线缓存后补传 |

### 2.6 验收标准（节选）

- AC-1：确定性告警 → 联动动作触发 P95 ≤ 1s（含通知/广播/门禁/视频）；
- AC-2：疑似告警 0 误联动（未确认不触发，演练验证）；
- AC-3：预案匹配准确率 ≥ 95%（事件类型×等级 → 正确预案）；
- AC-4：复盘时间线完整率 ≥ 99%（多源事件无丢失）；
- AC-5：断网演练下，本地规则引擎仍可完成 T0 联动（预案本地缓存）。

### 2.7 Agent 群与 Harness 需求补充（Spec 同步）

| 编号 | 需求 | 优先级 | 说明 |
|---|---|---|---|
| FR-A1 | 指挥员多轮对话 | P0 | 指挥助手 Agent 支持指挥员自然语言交互：查预案/查资源/问进展/生成指令建议 |
| FR-A2 | 现场协同对话 | P1 | 现场协同 Agent 与处置人员交互：任务确认/进展上报/引导（语音优先，弱网可用） |
| FR-A3 | 事件时间线可复盘 | P0 | 多源事件 append-only 融合，复盘报告自动生成（响应时限逐项评估） |
| FR-A4 | 演练脚本化 | P1 | 事件流脚本注入（时间轴），演练结果回流评测集（source=drill） |
| FR-A5 | Skill 化封装 | P1 | 预案匹配/复盘能力封装为 Skill（plan_match / drill_review），支持灰度发布与回滚 |
| NFR-A1 | T0 联动延迟 | P0 | 确定性告警 → 联动动作 P95 ≤ 1s，不依赖 LLM |
| NFR-A2 | 断网可用 | P0 | 规则引擎 + 预案缓存本地运行，T0 联动不依赖网络；恢复后补传 |
| NFR-A3 | 误联动门禁 | P0 | 疑似告警 0 误联动纳入发布门禁（演练验证，防坑#3 回退） |
| NFR-A4 | 工具副作用审计 | P0 | BROADCAST/WRITE 工具调用全量审计，支持撤回动作留痕 |
| NFR-A5 | 演练评估指标 | P1 | 响应时限/联动成功率/预案匹配率/时间线完整率四项演练指标进复盘 |
| NFR-A6 | 工具契约测试 | P1 | broadcast/unlock_door 等联动工具契约测试纳入 CI（不回归） |

---

## 3. 系统设计

### 3.1 总体架构

```
┌────────────────────────────────────────────────────────────────┐
│ 事件接入层：一键报警 / 电话 / App / 烟雾气体传感器 / 视频AI事件   │
│ 人工交互：报警人 / 值班员 / 指挥员 / 现场处置人员                │
└───────────────────────┬────────────────────────────────────────┘
                        ▼
┌────────────────────────────────────────────────────────────────┐
│ Agent 编排层（应急指挥 Agent 群：LangGraph 多 Agent + 治理扩展层）│
│  ├─ 事件识别 Agent：多源接入 → 事件类型/等级 → 疑似/确定性分级   │
│  ├─ 先期处置 Agent（T0，确定性优先，不依赖 LLM）：              │
│  │    联动编排：通知 → 广播 → 门禁 → 视频 → 预案加载（毫秒级）  │
│  ├─ 决策增强 Agent（T0+，并行）：LLM 风险分析 / 任务细化 / 指令  │
│  ├─ 指挥助手 Agent：与指挥员多轮对话（查预案/查资源/问进展）     │
│  ├─ 现场协同 Agent：与现场人员交互（任务确认/进展上报/引导）     │
│  ├─ 复盘 Agent：事件后时间线还原 / 响应评估 / 复盘报告           │
│  ├─ 共享记忆：事件状态机 + 处置时间线 + 各 Agent 会话上下文      │
│  └─ 协作机制：事件总线驱动 + 角色分工 + 状态共享 + 会话隔离      │
└────────┬───────────────────────────────────────────────────────┘
         ▼ 工具调用（MCP 风格：Schema 校验 + 权限 + 审计 + 副作用分级）
┌────────────────────────────────────────────────────────────────┐
│ 业务工具层（Java —— 联动执行底座，Agent 不可绕过）               │
│  ├─ 联动执行网关：广播/门禁/视频/短信/电话适配器（副作用分级）    │
│  ├─ 结构化预案库 / 通知名单 / 资源台账                          │
│  ├─ 确认窗口管理：疑似告警确认计时与超时策略                     │
│  └─ 时间线存储：多源事件 append-only 融合                        │
└────────┬───────────────────────────────────────────────────────┘
         ▼
┌────────────────────────────────────────────────────────────────┐
│ 数据层：事件库 | 结构化预案 | 通知名单 | 时间线事件存储          │
└────────────────────────────────────────────────────────────────┘
```

**Agent 群与业务工具层的对接机制（4 条，面试重点）：**

1. **边界原则——Agent 群是「决策编排」层，联动网关是「执行权威」层**：Agent 负责事件识别、处置编排、指挥对话、复盘分析；联动网关负责设备执行、副作用分级、失败记录。**铁律：高代价动作（广播/门禁）必须经网关执行 + 确认窗口**——Agent 不可绕过网关直接操作设备，疑似事件只能编排「低成本动作」（通知值班员确认），确定性事件才编排联动（对应 R-02）。
2. **对接方式——工具化 + 副作用分级（MCP 风格）**：`notify`、`broadcast`、`unlock_door`、`pull_video`、`load_plan`、`record_action`；工具按 Side-effect 分级（READ / WRITE / BROADCAST），BROADCAST/WRITE 工具强制「确认窗口 + 调用审计」（同时是项目二 Replay 桩化的依据）。
3. **多 Agent 协作——事件总线 + 角色分工**：事件识别 Agent 产出事件上下文（类型/等级/地点/确定性）写共享状态；先期处置 Agent（T0）与决策增强 Agent（T0+）并行执行互不阻塞；指挥助手 Agent 与现场协同 Agent 会话隔离（指挥员会话/现场会话分开），共享记忆只共享事件状态不共享会话原文（权限隔离）。
4. **确定性优先——T0 不依赖 LLM**：先期处置 Agent 的联动编排是规则驱动的确定性动作（毫秒级），LLM 只存在于 T0+ 增强 Agent 与对话型 Agent（指挥助手/现场协同）——「确定性优先，智能增强」在多 Agent 群里的体现，保障断网/模型故障时 T0 仍可用（本地规则 + 预案缓存）。

### 3.2 核心数据模型（DDL 节选）

```sql
CREATE TABLE incident (
  incident_id    BIGINT PRIMARY KEY,
  tenant_id      BIGINT NOT NULL,
  event_type     VARCHAR(32) NOT NULL,     -- FIRE / LEAK / INJURY / EQUIPMENT
  level          VARCHAR(16),              -- 一般/较大/重大
  source         VARCHAR(16),              -- MANUAL / GAS_SENSOR / VIDEO_AI / SMOKE
  certainty      VARCHAR(16),              -- DETERMINISTIC / SUSPECTED
  space_node_id  BIGINT,                   -- 事件地点（空间树）
  confirm_deadline TIMESTAMPTZ,            -- 疑似告警确认窗口截止
  confirmed_by   BIGINT, confirmed_at      TIMESTAMPTZ,
  state          VARCHAR(32) NOT NULL,     -- DETECTED / CONFIRMING / RESPONDING / CLOSED
  created_at     TIMESTAMPTZ, updated_at   TIMESTAMPTZ
);

CREATE TABLE incident_action (              -- 联动/任务动作（append-only 时间线）
  action_id    BIGINT PRIMARY KEY,
  incident_id  BIGINT NOT NULL,
  action_type  VARCHAR(32),                -- NOTIFY / BROADCAST / DOOR / VIDEO / TASK / LLM_ADVICE
  target       JSONB,                      -- 对象（名单/区域/摄像头/任务内容）
  status       VARCHAR(16),                -- TRIGGERED / SUCCEEDED / FAILED / REVOKED
  latency_ms   INT,                        -- 触发到完成延迟（复盘关键指标）
  operator     VARCHAR(64),                -- SYSTEM / 人工
  created_at   TIMESTAMPTZ
);

CREATE TABLE structured_plan (              -- 结构化预案库（坑#2 的产物）
  plan_id      BIGINT PRIMARY KEY,
  event_type   VARCHAR(32) NOT NULL,
  level        VARCHAR(16) NOT NULL,
  plan_name    VARCHAR(128),
  source_doc   VARCHAR(256),               -- 来源 Word（可追溯原文）
  action_list  JSONB NOT NULL,             -- [{action, target, owner, timeout, seq}]
  resource_map JSONB,                      -- 物资/器材配置
  version      VARCHAR(16), status         VARCHAR(16),
  verified_by  VARCHAR(64), verified_at    TIMESTAMPTZ   -- 人工校对（坑#2）
);
```

### 3.3 两级响应架构（本场景核心设计，见坑#1）

```
T0 确定性链路（规则引擎，毫秒级，不依赖 LLM）：
  事件识别分级（规则 + 快速分类模型，本地部署）
    → 确定性告警：直接触发联动动作（通知/广播/门禁/视频/预案加载）
    → 疑似告警：进入确认窗口（N 秒，🔴 如 60s），确认后才联动
  联动执行网关：各系统适配器（MCP 风格），失败重试 + 记录 latency

T0+ 增强链路（LLM，秒级，并行不阻塞）：
  预案摘要与处置建议（依据结构化预案 + 实时传感器）
  风险提示（如「泄漏点下风向人员需疏散」依据风向数据）
  任务清单细化为可执行指令（责任人+时限）
  → 全部动作写入 incident_action 时间线（可复盘、可追责）
```

### 3.4 预案结构化工程（坑#2 的完整方案）

```
输入：几百页 Word 预案（火灾/泄漏/伤害多事件混排、表格混排）
流程：
  1. NLP 初解析：标题层级 → 章节树 → 表格转结构化 → 动作句抽取
  2. 专家校对工具：可视化调整结构（拖拽归属、修正顺序）、确认动作清单
  3. 结构化入库：event_type × level → action_list（动作/对象/时限/责任人）
  4. 质量校验：与原文逐段对齐 + 覆盖性检查（每个事件类型都有预案）
产出：结构化预案库（人工校对，见坑#2 为什么不能纯自动）
```

### 3.5 技术选型表

| 组件 | 选型 | 理由（含行业约束） |
|---|---|---|
| 联动引擎 | 规则引擎（Java/Drools）本地部署 | 毫秒级 + 断网可用（R-01/R-10） |
| 事件分类 | 快速分类模型（轻量，本地） | 秒级识别不依赖大模型；大模型做增强 |
| 预案解析 | NLP 初解析 + 专家校对工具 | 结构化准确性优先（坑#2） |
| 联动网关 | 自研适配器（广播/门禁/视频/短信 MCP 化） | 统一执行 + 失败记录 + latency 度量 |
| LLM 增强 | Python 服务（私有化模型） | 处置建议/风险分析（辅助不阻塞） |
| 时间线 | 事件溯源式存储（append-only） | 复盘 + 追责完整还原 |
| 视频 | 视频中台接口（调取/云台） | 事件地点邻近摄像头自动调取 |
| 部署 | 私有化 + 本地缓存预案库 | 断网仍可 T0 联动 |

### 3.6 应急指挥 Agent 群行为设计（平台编排层在本场景的落地）

**事件识别 Agent（多源接入 → 分级）**
```
接入：人工上报（一键报警/电话/App）/ 传感器 / 视频 AI 事件
识别：事件类型（规则+快速分类，本地）→ 等级 → 地点（空间树解析）
分级：确定性（手动报警/气体超标）→ 直接联动；疑似（视频 AI 事件）→ 确认窗口
输出事件上下文（type/level/location/certainty）写共享状态（事件总线）
```

**先期处置 Agent（T0，确定性优先，不依赖 LLM）**
```
收到确定性事件上下文 → 流程模板（规则驱动，毫秒级）：
  通知（notify，按 R-03 名单）→ 广播（broadcast，按 R-05 话术）
  → 门禁解锁（unlock_door，疏散门）→ 视频调取（pull_video，邻近摄像头）
  → 调用 plan_match Skill（事件类型×等级 → 结构化预案，预加载到指挥台）
动作全部经联动网关执行（副作用分级 + 失败记录 + latency），不直接操作设备
```

**决策增强 Agent（T0+，并行不阻塞）**
```
与 T0 并行：预案摘要与处置建议（LLM，依据 plan_match Skill 输出 + 实时传感器）
风险提示（如泄漏下风向疏散，依据风向数据工具）
任务清单细化（动作→责任人→时限，写入 incident_action 时间线）
延迟要求：建议类输出秒级可用；即使失败/超时也不影响 T0 已触发的联动
```

**指挥助手 Agent（与指挥员多轮对话）**
```
意图：查预案 | 查资源 | 问进展 | 生成指令建议
能力：plan_match Skill（预案问答）+ query_resource / query_timeline / generate_order
答复引用预案与时间线事实，指令建议标注「建议，需指挥员确认后执行」
```

**现场协同 Agent（与现场处置人员交互，弱网优先）**
```
任务下发（确认/认领）→ 进展上报（语音/文字/照片，离线缓存后补传）
引导（疏散路线/集合点，基于空间树）→ 上报内容入事件时间线
```

**复盘 Agent（事件后自动分析，drill_review Skill）**
```
调用 drill_review Skill（内部 pipeline：时间线还原（多源事件融合，缺时间戳标记存疑）
→ 响应评估（接警→先期处置耗时 vs R-06；联动成功率；预案匹配率）
→ 复盘报告生成（处置时间线图示 + 改进建议））
结果回流评测集（source=drill），支持演练与实战两用
```

### 3.7 Harness 应用（演练注入与联动可靠性评测）

```
演练注入（脚本化事件流）：
  时间轴脚本：告警→上报→传感器异常→处置动作（可注入边界场景：
  确定性/疑似事件、地点解析失败、联动系统故障、断网）
评估指标（复盘自动产出）：
  响应时限（接警→先期处置 ≤ X）｜ 联动成功率 ｜ 预案匹配率 ｜ 时间线完整率
L1 Skill 评测（plan_match / drill_review）：
  绑定用例（预案匹配准确率、复盘报告完整性、响应评估正确性）→ 变更门禁
  Skill pipeline/预案版本变更 → 自动触发绑定用例回归
L0 工具契约（broadcast / unlock_door / pull_video / notify）：
  契约测试（schema/参数/错误码）+ BROADCAST 桩化计数校验（=0）
门禁（发布前）：
  疑似告警 0 误联动 ｜ T0 联动 P95 ≤ 1s ｜ 断网演练 T0 仍可用
轨迹回放：
  处置全链路录制 → Skill/工具桩化（BROADCAST/WRITE 强制桩化 + 调用计数=0 校验）
  → 变更注入（联动规则/plan_match 版本/确认窗口时长）→ 指标 diff
数据回流：误报案例 / 处置复盘 → 评测集扩充 → 规则与预案优化
```

---

## 4. 实现交底（分模块实现要点）

| 模块 | 实现要点 |
|---|---|
| 事件识别分级 | 规则优先（手动报警=确定性；传感器超阈值=确定性）；视频 AI 事件=疑似（置信度低/场景误报率高时段降权）；分级依据规则 + 人工确认 |
| 确认窗口 | 疑似告警 → 推送给值班员（App/短信/语音）→ 确认/忽略；超时未确认 → 升级提醒（不自动联动）；确认后按确定性流程走 |
| 联动执行网关 | 每个系统一个适配器（广播/门禁/视频/短信/电话）；动作失败 → 重试 1 次 → 失败记录 + 人工替代路径提示；全部动作记录 latency（复盘输入） |
| 预案加载 | 事件识别完成即加载预案（预加载到内存）；指挥台一屏：时间线 + 视频 + 任务清单 + 预案动作 |
| 时间线融合 | 多源事件（告警/上报/动作/传感器/LLM 建议）统一时钟写入；缺时间戳事件标记存疑；复盘报告自动生成（响应时限逐项评估 vs R-06） |
| 断网降级 | 规则引擎 + 预案库 + 通知名单本地缓存；恢复联网后事件/动作补传 |

---

## 5. 实现交底补充：与内嵌支撑能力的关系

| 内嵌能力 | 在本场景的位置 |
|---|---|
| 记忆（任务状态/情景） | 事件状态机 + 处置时间线（处置到哪一步、谁负责什么、已完成什么） |
| 记忆（短期） | 指挥对话中的上下文（值班员与系统多轮交互） |
| 知识（预案库/案例库） | 结构化预案 + 历史同类事件处置复盘案例（增强建议引用） |
| 数据闭环/评测 | 演练评测：注入事件 → 评估响应时限/联动成功率/预案匹配率，进发布门禁 |
| 语义化数据分析 | 事件类型识别、上报文本提取（地点/类型/伤亡描述） |

---

## 6. 踩坑与解法（业务特点驱动的深坑，精而深）

### 坑 #1：LLM 生成处置建议太慢——8 秒的「正确建议」在火灾现场一文不值

**背景**：应急指挥 v1 采用「LLM 端到端生成先期处置建议」（识别→生成→下发），演练前没有做性能量化。
**现象**：第一次全要素演练，实测从告警到「通知+疏散指令」端到端 **8–15 秒**（含 LLM 推理与链路）。复盘时指挥员一句话点醒：「建议内容没问题，但火灾里这 8 秒，人已经跑不到门口了。」——黄金处置时间是秒级，LLM 的推理延迟在应急场景是**结构性缺陷**，不是调优能解决的。
**排查**：拆解链路耗时：事件分类 0.8s + LLM 生成 6–12s + 联动下发 1–2s。LLM 生成占绝对大头；且「生成完才联动」的串行设计放大了延迟。
**解决**：重构为**两级响应架构**（§3.3）：
1. **确定性动作不走 LLM**：规则引擎毫秒级直接触发通知/广播/门禁/视频/预案加载（这些动作在事件类型确定后就是确定的，不需要「思考」）；
2. **LLM 降级为增强层**：并行生成风险分析、任务细化建议，作为指挥台辅助信息异步展示，不阻塞任何确定性动作；
3. **预案预加载**：事件类型识别（快速模型）完成的瞬间即预加载对应预案到内存，T0 联动与预案加载并行；
4. **性能指标入验收**：响应时限（R-06）成为演练评测的硬指标，每次演练自动评估「接警→先期处置动作」耗时。
**反思**：应急场景的架构铁律是「**确定性优先，智能增强**」——先保证系统 100ms 内做出正确动作，再让 LLM 加分；把 LLM 放在决策主链路上，等于给黄金时间上了个「思考税」。这个原则后来被我带进了所有强时效业务。

### 坑 #2：预案解析「结构对、语义错」——纯 NLP 解析出的预案根本不能用

**背景**：预案结构化初期，用标题层级 NLP 解析几百页 Word，期望全自动入库。
**现象**：抽样校验发现解析结果「结构完整但语义错乱」：一份 Word 里火灾、泄漏两个事件的预案**混排在同一章节树**，NLP 按标题层级切分后段落归属错误（把「3.2 处置步骤」归给了泄漏，实际是火灾的）；表格混排导致动作顺序颠倒；解析出的动作清单执行顺序与原文相反。若直接用，匹配到错误预案或动作颠倒，演练必出事。
**排查**：根因是**预案文档本身写作不规范**——行业预案没有统一模板：有人按事件分章、有人按阶段分章、有人一段写多个事件的处置、表格里嵌步骤。NLP 只能解析「标题结构」，解析不了「语义归属」。
**解决**：
1. **解析 + 人工校对双轨**：NLP 初解析 → **专家校对工具**（可视化拖拽调整章节归属、修正动作顺序、确认责任人）→ 结构化入库；纯自动解析只作初稿；
2. **预案 schema 化**：强制 event_type × level → action_list（动作/对象/时限/责任人）结构，入库前 schema 校验（动作清单非空、时限合法）；
3. **质量校验**：与原文逐段对齐 + 覆盖性检查（每个事件类型×等级都有预案），校验不过不允许发布为「可用预案」；
4. 后续新预案强制用「结构化填写模板」产出（从源头消灭非结构化）。
**反思**：行业里最大的价值洼地往往是**数据工程**——几百页 Word 到结构化预案的转换，难的不是 NLP 而是「文档不规范 + 语义归属要人确认」；这个认知让我后来遇到所有「非结构化→结构化」需求，第一反应都是「先设计人机协同的校对流程，再谈模型」。

### 坑 #3：视频 AI 的「烟雾误报」差点全园区广播——疑似/确定性分级救回来的

**背景**：联动规则上线时，把视频 AI 的「烟雾检测」事件直接接入联动（当成确定性火灾）。
**现象**：上线两周内，视频 AI 将「车间蒸汽」「施工扬尘」「焊烟」识别为烟雾事件 3 次，其中 1 次直接触发了全园区疏散广播——事后确认是蒸汽，但广播已经播了。园区运营方当场叫停联动功能：**误报驱动的联动会摧毁信任**（狼来了）。
**排查**：视频 AI 烟雾检测是视觉模型的概率输出，在蒸汽/扬尘/焊烟场景误报率高；把「疑似信号」当「确定性事实」直接驱动高代价动作（全园广播），是设计错误。
**解决**：
1. **联动分级**（R-02）：**确定性告警**（手动报警、气体浓度超阈值）→ 直接联动；**疑似告警**（视频 AI 事件、单一传感器）→ 进入**人工确认窗口**（值班员 N 秒内确认，🔴 如 60s），确认后才触发联动；
2. **多源交叉确认**：疑似事件 + 第二个独立信号（如温度异常/烟感/人工上报）命中 → 提升为确定性；
3. **高误报时段降权**：雨雾/施工扬尘时段，视频 AI 事件置信度整体降权，更依赖人工确认；
4. **广播撤回能力**（R-09）：广播类动作支持撤回/更正，误触发后 30s 内可撤回并记录（补救但不依赖补救）。
**反思**：应急联动的核心矛盾不是「快」而是「**准**」——高代价动作（全园广播）必须由「确定性信号」驱动；疑似信号只能驱动「低成本动作」（通知值班员确认）。把「确认窗口」做进架构而不是靠 Prompt，是这类系统的生死线。

---

## 7. JD 对标收口（本场景覆盖的技能点）

| JD 技能点 | 落点 |
|---|---|
| Agent 数据闭环 | 事件→处置→复盘全链路 + 演练评测回流 |
| 短期/任务状态记忆 | 事件状态机 + 处置时间线（处置到哪、谁负责什么） |
| 上下文工程（MCP Tools） | 广播/门禁/视频/短信工具化接入（联动执行网关） |
| 语义化数据分析（意图/聚类） | 事件类型识别、上报文本结构化提取 |
| 评测（评测集/自动评估/演练） | 演练注入评测：响应时限/联动成功率/预案匹配率 |
| 知识（预案库/案例库） | 结构化预案 + 历史处置复盘案例 |
| 多租户/审计 | 事件/动作全量留痕，复盘追责 |
| Python+SQL/Java | 事件分类/LLM 增强 Python、联动引擎/时间线 Java |

---

## 8. 面试素材

### 高频追问
1. 应急为什么不用 LLM 直接生成指令？→ 8 秒延迟 vs 黄金时间的教训（坑#1），确定性优先
2. 预案怎么结构化的？→ NLP 初解析 + 专家校对双轨 + schema 化（坑#2）
3. 怎么防误联动？→ 疑似/确定性分级 + 人工确认窗口 + 多源交叉（坑#3）
4. 断网怎么办？→ 规则引擎/预案/名单本地缓存，恢复后补传
5. 复盘怎么做？→ 多源事件时间线融合 + 响应时限逐项评估

### 一句话素材
> 应急指挥让我建立了一条架构铁律：强时效业务里「确定性优先，智能增强」——规则引擎 100ms 内做对的事，LLM 只做锦上添花的增强；而视频 AI 误报的教训告诉我，高代价动作只能由确定性信号驱动，疑似信号必须走人工确认窗口。
